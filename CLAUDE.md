# CLAUDE.md — 작업 규약

SDKB 는 **배포되는 연구 자산**이다. 논문 한 편의 부속물이 아니라, 여러 연구·응용이 그 위에 서 있는
지식 기반(substrate)이다 — CDLA-Permissive-2.0 으로 공개되고, `CITATION.cff` 로 인용되며,
공개 사이트가 이 그래프를 그린다.

그래서 이 저장소의 결함은 **조용히 하류로 전파된다.** 여기서 컬럼 하나가 잘못된 의미를 담으면,
그것을 쓰는 논문의 통계가 통째로 무효가 된다. 이 규약의 나머지는 전부 그 한 문장의 각주다.

---

## 0. 하류 소비자 (변경 전에 반드시 떠올릴 것)

| 소비자 | 무엇을 가져가는가 | 깨지면 |
|---|---|---|
| **하류 분석 저장소** (`sdkb-prior-art-paper`) | 특정 커밋의 TTL 을 **얼린 스냅샷**으로 vendor → 평가의 "보강 전 그래프" | 그쪽 baseline 이 움직여 보강 전후 비교가 재현 불가 |
| **SDKB-Match (Expert / PriorArt)** | 매칭·선행기술검색의 지식 기반 | 매칭 결과·GT 평가가 흔들림 |
| **공개 사이트 / 데이터셋 이용자** | 릴리스된 그래프 | 인용된 데이터가 사후에 달라짐 |

하류는 커밋 SHA + sha256 으로 우리를 **핀(pin)** 한다. 즉 우리가 이미 배포한 것을 제자리에서 바꾸면
그들의 출처 기록이 거짓이 된다. **어휘·IRI·의미를 바꾸는 변경은 CHANGELOG 와 버전으로 알린다.**

작업을 시작하기 전에 스스로 답하라: **이 변경은 위 표의 누구를 움직이는가?**

---

## 1. 절대 금지 (예외 없음)

1. **TTL 을 손으로 고치지 않는다.** `ontology/sdkb-core.ttl`·`sdkb-core-data.ttl`·`sdkb-abox-patents.ttl`
   은 **gitignore 된 빌드 산출물**이다. 진실의 원천은 `data/**` 의 JSON/JSONL/parquet 과
   `scripts/` 의 생성기다. 그래프가 틀렸으면 **생성기나 원천을 고친다.** TTL 을 고치면 다음 빌드에
   조용히 사라지고, 그 사이에 vendor 해 간 하류는 유령 데이터를 갖게 된다.
2. **ABox 에서 어휘를 인라인 선언하지 않는다.** 술어·클래스는 TBox(`sdkb-*.ttl` 을 만드는 생성기)에
   정의한다. ABox 파일 안에서 `owl:ObjectProperty` 를 선언하면 TBox 를 읽는 소비자에게는
   **존재하지 않는 술어**가 되고, SHACL·추론기가 검증할 수 없다.
3. **이름이 의미와 다른 필드를 만들지 않는다.** 컬럼·술어의 이름은 그 값이 실제로 무엇인지와
   일치해야 한다. 확신이 없으면 **권위 원천(KIPRIS 등)과 대조해 확인**하고, 확인 전에는 쓰지 않는다.
   (이 규칙은 실제 사고에서 나왔다 — §8 참조.)
4. **수치를 지어내지 않는다.** README·문서·데이터카드의 모든 숫자는 실행된 코드의 출력이어야 한다.
   추정치는 "추정"이라고 라벨링한다. 규모가 바뀌면 README·CHANGELOG 를 같은 커밋에서 고친다.
5. **원문 데이터를 재배포하지 않는다.** KIPRIS 수집물은 학술 이용·비재배포 조건이다.
   커밋 가능한 것은 집계·메타데이터·파생 구조뿐. 라이선스가 다른 데이터를 섞을 때는
   `dcterms:license` 로 출처별 조건을 반드시 표기한다(shapes 가 이미 요구한다).
6. **게이트를 우회하지 않는다.** SHACL 위반을 없애려고 shape 를 느슨하게 만들지 않는다.
   shape 가 틀렸다고 판단되면 **고치기 전에 사람에게 묻는다** — shape 는 하류와의 계약이다.
7. **비밀정보 하드코딩 금지.** API 키는 환경변수/`.env` 로만.
8. **요청하지 않은 코드 금지.** 리팩터링, 추상화, "나중에 쓸" 유틸리티, 승인된 계획에 없는 파일 —
   전부 쓰지 않는다. 필요해 보이면 **제안하고 멈춘다.**

---

## 2. 개발 절차 — 5단계와 정지 게이트

**🛑 는 사용자 승인 없이 넘어갈 수 없는 정지선이다.** 침묵은 승인이 아니다.

### 1단계 · 요구 정의  🛑
코드 없이 한 화면으로 제시하고 승인받는다.

```
목적      : 무엇을 고치는가. 하류(§0)의 누구를 위해서인가
입력      : 어떤 원천 파일·API. 스키마와 규모
출력      : 어떤 생성기가 무엇을 내놓는가. TBox/ABox/shape 중 무엇이 바뀌는가
성공 기준 : 무엇이 나오면 성공인가 (게이트 통과 형태로)
비목표    : 이번에 하지 않는 것
파급      : 어휘·IRI·의미가 바뀌는가? 그렇다면 CHANGELOG·버전·하류 통보 계획
```

### 2단계 · 분석  🛑
**원천을 먼저 관찰한다.** 형태를 모르는 채로 설계하지 않는다.
- 실제 파일을 열어 스키마·결측·분포·엣지케이스를 확인하고 **사실을 보고**한다.
- 값이 이름대로인지 **권위 원천과 대조**한다 (§1.3).
- 가정은 "가정"이라고 라벨링한다. 여기서 나온 사실이 1단계와 충돌하면 **되돌아간다.**

### 3단계 · 설계  🛑
- 어느 생성기(`scripts/*.py`)에 무슨 변경을, 어떤 시그니처로 넣을지 명시.
- TBox 를 바꾸는가? shape 를 바꾸는가? IRI 규칙을 바꾸는가? → **하류가 깨진다. 별도로 승인받는다.**
- 결정적(deterministic)인가. 같은 원천 → 같은 그래프인가. 난수·시각·해시 순서에 흔들리지 않는가.
- 신규 의존성은 여기서 근거와 함께 승인받는다.

### 4단계 · 구현
- 승인된 범위 안에서만. 설계에 없던 것이 필요해지면 **멈추고 3단계로 돌아간다.**
- 주석은 "왜"만 쓴다. 네임스페이스·경로는 `config/namespaces.py` 에서 가져온다.

### 5단계 · 테스트 및 검증  🛑
`make test` 통과가 최소 조건이고, 그 위에 아래 3층을 쌓는다.

**(a) 단위 테스트** — 순수 로직의 계약. 경계·결측·빈 입력·중복을 함께 넣는다.
외부 의존(KIPRIS API, 파일시스템, 시각)은 **격리**한다 — 네트워크를 타는 테스트는 단위 테스트가 아니다.

**(b) 통합 테스트 — 이 저장소의 주된 실패 양식**
단위 테스트가 전부 통과해도 **경계에서 깨진다.** 아래를 각각 고정한다.

| 경계 | 깨지는 방식 | 테스트가 고정할 계약 |
|---|---|---|
| 원천 데이터 → 생성기 | 컬럼이 이름과 다른 값을 담음 | 필드 의미 계약 (권위 원천 대조) |
| 생성기 → TBox | ABox 가 TBox 에 없는 술어를 씀 | **ABox 의 모든 술어가 TBox 에 정의되어 있는가** |
| TBox/ABox → shapes | shape 가 실물 그래프에 걸린 적이 없음 | **shape 를 그것이 겨냥하는 실제 그래프에 적용** |
| 그래프 → 하류 | 어휘·IRI 가 조용히 바뀜 | 릴리스 서명(클래스·술어·인스턴스 수) |

- 계약을 바꾸는 변경은 **양쪽 테스트를 같은 커밋에서** 고친다. 한쪽만 고쳐 통과시키는 것은 정합성을 깬 것이다.
- **실패해야 할 입력이 실패하는가**도 반드시 확인한다. 위반 델타를 넣었을 때 게이트가 거부하지
  않으면 그건 게이트가 아니다.

**(c) 릴리스 게이트** — §4. 그래프를 바꿨으면 예외 없이 실행하고 **실제 출력과 함께** 보고한다.
실패는 실패로 보고한다. 검증 없이 "완료"라고 말하지 않는다.

> 규모 예외: 오타·문서 문구 같은 사소한 변경은 1–3단계를 한 문장으로 압축해도 된다.
> **원천 데이터·생성기·TBox·shape 를 바꾸는 변경에는 예외가 없다.**

---

## 3. 모듈 배치표 — 새 코드가 갈 곳

| 위치 | 책임 | 넣지 말 것 |
|---|---|---|
| `data/` | **진실의 원천.** 수집물(JSON/JSONL/parquet)과 큐레이션 산출 | 파생 그래프 |
| `scripts/` | 생성기: ingest → parse → build_owl → convert → build_abox_* → align | 분석·해석 |
| `ontology/` | **빌드 산출물(TTL).** 손으로 고치지 않는다 | 수작업 편집 |
| `validation/` | SHACL shapes — 하류와의 구조 계약 | 데이터 수정 |
| `mappings/` | 코드 ↔ 개념 룰 테이블 | 하드코딩된 매핑 |
| `provenance/` | PROV-O 활동·출처 기록 | — |
| `tests/` | 단위 + 통합 | — |
| `notebooks/` | 탐색·서술. **확정 로직은 반드시 `scripts/` 로 승격** | 최종 산출 로직 |
| `docs/` | 데이터카드·거버넌스 | 코드 |

노트북에서 확정된 로직이 `scripts/` 로 올라가지 않으면 재현성이 깨진다 — 노트북은 초안, `scripts/` 가 정본이다.

---

## 4. 릴리스 게이트

**shape 는 그것이 겨냥하는 실제 그래프에 걸려야 한다.** 쓰여만 있고 돌지 않는 shape 는 게이트가 아니라 장식이다.

```bash
make owl convert          # 원천 → TBox/ABox 재생성 (TTL 은 여기서만 만들어진다)
make abox-patents         # 특허 ABox 재생성
make validate             # SHACL — 대상 그래프와 shape 를 명시적으로 짝지어 실행할 것
make test                 # 단위 + 통합
```

최소 요구:
- `validation/shapes.ttl` × `ontology/sdkb-core-data.ttl`
- `validation/shapes_patent.ttl` × `ontology/sdkb-abox-patents.ttl` ← **현재 배선되어 있지 않다(§8)**
- ABox 의 모든 술어가 TBox 에 정의되어 있는가 (인라인 선언 탐지)
- 재현성: 같은 원천으로 두 번 빌드해 **동일한 그래프**가 나오는가

릴리스(하류가 vendor 해 갈 커밋)를 만들 때는 **그래프 서명**(클래스별 인스턴스 수, 술어별 트리플 수)을
`CHANGELOG.md` 에 남긴다. 하류는 이 숫자로 자기 스냅샷을 검증한다.

---

## 5. 데이터 프로파일 의무 (수집·변형 산출물의 필수 동반물)

**데이터를 수집하거나 변형한 뒤에는 예외 없이** 아래를 산출하고 보고한다. "데이터 받았습니다"는 미완이다.

1. **구조** — 컬럼명, dtype, 단위, **각 컬럼의 의미와 원천 필드**(어느 API 의 어느 태그인가), 키
2. **형태** — 행×열, 중복, 컬럼별 결측률, 기간 범위, dedup 전/후
3. **기술통계** — 수치형: count·mean·std·min·median·max / 범주형: 고유값·상위 빈도 / 시간축: 연도별 건수
4. **의미 검증** — **컬럼 값이 이름대로인가를 권위 원천과 대조**한다. 특히 날짜: 출원일·공개일·등록일은
   서로 다르며, 섞이면 시계열 연구가 통째로 무효가 된다.
5. **사용 목적** — 이 데이터가 어느 그래프의 어느 술어가 되는가. 쓰이지 않는 컬럼은 왜 남기는가

프로파일은 **코드가 생성한다**(손으로 쓰지 않는다). 놀라운 점(이상치·결측 편중·기간 공백)이 있으면
다음 단계로 넘어가기 전에 사용자에게 알린다.

---

## 6. 명령 레퍼런스

```bash
make parse            # 원천 JSON → 정규화
make owl              # TBox 빌드
make convert          # RDF 직렬화
make abox             # 코어 ABox
make abox-patents     # 특허 ABox (ingest-sirp 선행)
make ingest-sirp      # SIRP 거절특허 적재
make align            # 정렬 후보
make validate         # SHACL
make test             # pytest
```

커밋은 사용자가 요청할 때만. 커밋 메시지는 **어느 소비자(§0)에게 무엇이 달라지는지**를 밝힌다.

---

## 7. 보고 방식

- **결론 먼저.** "무엇이 나왔는가"를 첫 문장에.
- 수치에는 **출처 명령/파일**을 함께 적는다.
- 불확실한 것은 불확실하다고 말한다.
- **하류를 깨는 변경, 실패한 검증, 건너뛴 단계는 반드시 먼저** 알린다.
- 사용자는 연구자다. 코드를 붙여넣기보다 **그 코드가 만든 증거**를 보여준다.

---

## 8. 현재 알려진 부채 (2026-07 확인, 근거 있는 사실)

아래는 추정이 아니라 실행으로 확인된 결함이다. 정비 중이며, 새 코드가 이것을 **답습하지 않도록** 적는다.

1. **`rejected_patents_meta.parquet` 의 `filing_date` 는 출원일이 아니라 공개일이다.**
   KIPRIS 권위 조회 결과 출원번호 `10-2021-0184131` 의 `applicationDate` 는 2021-12-21,
   `openDate` 는 2023-06-28 인데 parquet 의 `filing_date` 에는 **2023-06-28**(공개일)이 들어 있다.
   `filing_date == publication_date` 가 99%, 출원번호가 인코딩한 출원연도와의 일치율은 34% 다.
   → 시계열 연구(기술예측)에서 시점이 1~2년 밀린다. **§1.3 규칙이 여기서 나왔다.**
2. **특허 ABox 가 TBox 에 없는 술어를 쓴다.** `ont:concernsProcess`(1,558 트리플)는
   `sdkb-patent.ttl` TBox 에 정의가 없고 ABox 안에서 인라인 선언된다. TBox 가 정의한 것은
   `ont:realizesProcess`(domain Patent, range Process) 다. `primaryIpc` 도 마찬가지로 TBox 어휘
   (`hasIPC`)와 어긋난다.
3. **특허 인스턴스에 `ont:filingDate` 가 0% 다.** 1,000건 전부 출원일 술어가 없다.
4. **patent shape 가 실물 특허 ABox 에 걸린 적이 없다.** `validation/shapes_patent.ttl` 을
   `ontology/sdkb-abox-patents.ttl` 에 적용하면 **3,000건 위반**(`dcterms:license`·`dcterms:source`·
   `prov:wasGeneratedBy` 각 1,000건)이 나온다. `make validate` 의 기본 대상이 코어 데이터 그래프라
   특허 ABox 는 검증되지 않았다.

**SIRP 는 1,000건이다** (초기 코호트 스냅샷이 773건이었고 GT 페어가 그 시점에 동결되어 있다 —
두 숫자를 혼동하지 말 것).
