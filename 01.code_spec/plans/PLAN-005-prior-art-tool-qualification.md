# PLAN-005 — 선행기술요소 조사 도구 적격성: 진단 · 재구성 · 검증 (§2 1단계 🛑)

> **지위: 요구 정의 · 승인 대기 🛑.** 근거 규약은 [CLAUDE.md §2](../../CLAUDE.md) 5단계의
> **1단계**이며, **이 파일이 저장소에 있다는 사실을 승인으로 읽지 않는다.** T-Box·shape·IRI 를
> 바꾸는 항목은 §2 3단계에서 **별도로** 승인받는다.
>
> **출처 (2026-09-05 · 사용자 지시).** 이 계획의 출처는 **하류 문서가 아니다.** PLAN-001~004 가
> 전부 하류 `sdkb-prior-art-paper` 의 계획에서 이관된 것과 달리, 이 계획은 사용자가 이 저장소에
> 직접 낸 요구에서 출발한다:
>
> > *"선행기술조사 업무 지원을 위한 온톨로지가 의미검색이 가능하도록 제대로 된 추론 공리가
> > 적용되어 구축되어 있는가. 선행기술조사의 클래스·프로퍼티가 너무 특허 종속적인 것 아닌가.
> > 신규성·진보성·FTO 라는 명확한 업무 목적을 달성 가능하도록 온톨로지가 정의·구성되어 있고
> > 적절한 실험과 검증으로 확인되었는가."*
> >
> > *"특허 데이터를 사용하여 정답지를 만들어 쓰지만, 특허가 아니라 연구노트일지라도 선행기술
> > 요소가 조사되는, 도메인에서 특허·논문 등 지식자산 신규 산출 시 사용 가능한 도구가 되도록
> > 개선하려고 한다. 하류 하네스에 종속되지 않고 상류에서 독립적으로 적용되도록 계획할 것."*
>
> **그러므로 이 계획은 상류에서 완결한다.** 하류의 규약·사전등록·봉인·판정에 종속되지 않으며,
> 하류 저장소의 어떤 파일도 바꾸지 않는다.

---

## 0. 결론 먼저

**현 SDKB 는 선행기술요소 조사 도구가 아니라 "심사 결과 대장 + 조립 태그"다.** 어휘는 판단
대상을 담지 못하고, 공리는 확장을 만들지 못하며, 검증은 판단 능력을 잰 적이 없다. 근거는 §2 이며
전부 이 저장소의 워킹트리에서 직접 센 값이다.

**그리고 그 진단은 PLAN-001 이 이미 내린 것과 같다** — 이 계획이 더하는 것은 셋이다.

1. **업무 목적에서 역산한 판정 기준** — "공리가 몇 개인가"가 아니라 "신규성·진보성 판단을
   지원하는가".
2. **무엇이 OWL 로 되고 무엇이 안 되는가의 판정**(§3) — 집합층은 OWL 밖이며, 그 사실이
   재구성의 형태를 정한다. PLAN-001 §1.3 은 이 구분을 적고 있지 않다.
3. **"특허 종속"을 기계로 검사하는 규칙**(§3.4) — 이 규칙 하나가 *"연구노트도 질의가 되는가"*
   에 대한 검증 가능한 답이 된다.

---

## 1. 1단계 요구 정의 (§2 서식)

**목적.**
어떤 기술요소 조합을 입력하면 그 조합을 이미 개시한 선행기술을 찾아 주는 도구로 SDKB 를
적격화한다. 입력이 **청구항이든 연구노트든 같은 경로로** 처리되어야 한다. 수혜자는 §0 의 하류
소비자가 아니라 **이 자원을 도메인에서 실제로 쓰는 사람**이며, 하류는 필요할 때 얼린 스냅샷을
가져간다.

**입력.**

| 원천 | 규모 | 용도 |
|---|---|---|
| `ontology/*.ttl` (T-Box 1,577 트리플 · A-Box 11.9M) | §2.2·§2.3 | 진단·재구성 대상 |
| `data/sources/opinion_notices/txt` | **1,155 문서 / 999 출원** | 공리 원천 · 신규성 축 회복 |
| `data/patents/rejection_decisions/structured` | 441 | 근거 조항 · 인용 해소 |
| `data/patents/prior_art_edges.parquet` | 6,692행 (examiner 2,534) | 정답지 |
| `mappings/claim_features.parquet` | 1,306,191행 | 요소 집합의 원천 |
| `queries/cq/*.rq` · `validation/shapes*.ttl` | 31 · 33 NodeShape | 검증 자산 |

**출력.**

| 생성기 | 산출 | 무엇이 바뀌는가 |
|---|---|---|
| 신설 계수 스크립트 | V1–V4 기준선 (`data/reports/`) | **아무것도 바꾸지 않음** (읽기 전용) |
| 기존 판단 추출 경로 교정 | 신규성 축 회복 | A-Box |
| 통지서 인용 해소 생성기 | 근거가 붙은 판단 | A-Box |
| `build_owl.py` 확장 | `sdkb-claim-semantics.ttl` | **T-Box + R-Box** (별도 승인 🛑) |
| 신설 태스크 질의 | `queries/cq/` | 검증 자산 |

**성공 기준.** §5 의 V1–V4 가 각각 **기준선**과 **결과를 보기 전에 동결한 목표**를 갖고, 최종
재측정이 그 목표와 대조된다. 특히:

- **V1** — 자원에 남은 R-Box 공리 중 소비 경로가 확인되지 않은 것이 **0건**.
- **V2** — `SemanticPathRecall@S` 가 기준선 대비 유의 상승 **∧** 절대 하한 통과 **∧**
  `|Reach(q)|` 중앙값이 증가하지 않음(셋 모두).
- **V3** — 진보성 하위집단에서 조합 커버가 단일 최대를 유의하게 넘음.
- **V4** — §3.4 검사 규칙이 CI 에서 돌고, 태스크 질의가 행정 어휘를 필수부에 쓰면 실패함.
- **V6** — (2026-09-05 신설 · 안 C′-full) **V6a** 바이오 소코호트 실물 이식에서 `pa:` core
  변경 **0줄** · **V6b** US 종이 이식에서 core 변경 **0줄** 로 태스크 질의·집합층 SHACL 이
  무수정 통과 · **CI 불변식**: `sdkb-priorart-core.ttl` 에 도메인 IRI 또는 관할 IRI 가
  등장하면 실패.

**비목표.** §7.

**파급.**
어휘·IRI 가 **추가**된다. 기존 `sdkb-patent.ttl` 은 **한 줄도 고치지 않고** 새 모듈을
`owl:imports` 로 결합하므로(PLAN-001 §1.2 설계 그대로), §0 의 하류 소비자 계약은 **구조로**
지켜진다. 단계 4 착수 시 CHANGELOG·버전·하류 통보를 동반한다.

---

## 2. 진단 — 워킹트리 실측 (2026-09-05)

### 2.1 어휘가 판단이 아니라 행정을 서술한다

선행기술 어휘는 `Patent` · `RejectedPatent` · `CitedPatent` · `Claim` · `ClaimFeature` ·
`PriorArtJudgment` · `RejectionType` 다. **기술요소 · 요소의 조합 · 조합 사이의 포함 관계 ·
치환 가능성 · 결합 용이성을 1급 시민으로 가진 클래스는 0개다.**

**증상 ①** — `featureConcept` 의 range 7클래스(Process ⊔ SubProcess ⊔ Device ⊔ Material ⊔
Skill ⊔ FailureMode ⊔ EquipmentClass) 중 뒤의 셋은 FMEA·전문가매칭 계열이다. 그래서 청구항에
가장 흔한 **구조요소**(게이트·기판·드레인·배선 등 15개)가 `data/reports/ko_concept_proposals.json`
에 *"축 부재 — 구조 요소를 담을 클래스가 T-Box 에 없다"* 는 이유로 **등재 보류**되어 있다.
어휘가 판단 대상을 담지 못한다는 사실을 자원 스스로 기록해 둔 것이다.

**증상 ②** — 스스로 *"IP-R&D 선행기술조사의 핵심 질의"* 라 부르는
`queries/cq/CQ10_prior_art_candidates_by_concept.rq` 의 본문은
`?prior a ont:Patent ; ont:realizesProcess <…/plasma_etch> ; ont:filingDate ?f . FILTER(?f < …)`
이다. **개념 1개 + 날짜 필터.** 집합도 포함도 없고, 무엇보다 **질의가 특허여야 한다.**

### 2.2 R-Box 가 사실상 없다

SDKB 자체 T-Box 의 추론 공리 전량은 **8건**이다.

| 공리 | 건수 | 비고 |
|---|---:|---|
| `owl:TransitiveProperty` | 2 | `hasSubStep` · `broaderClassification` |
| `rdfs:subPropertyOf` | 5 | `hasPriorArtExaminer ⊑ hasPriorArt` 등 |
| `owl:equivalentClass` | 1 | `Dopant ≡ Acceptor ⊔ Donor` — 특허와 무관 |
| `inverseOf` · `propertyChainAxiom` · `disjointWith` · `hasKey` · cardinality · `Restriction` | **각 0** | `Restriction` 15건은 전부 SemicONTO import 안 |

클래스 계층은 최대 깊이 3이고 명명 클래스의 **47%가 계층에서 고립**돼 있다.

결정적인 것은 규모가 아니라 **무엇이 없는가**다. `dependsOnClaim` 이 전이가 아니어서 **"종속항을
포함한 완전 한정요소집합"조차 추론으로 낼 수 없다.** 소스 주석(`ontology/sdkb-patent.ttl:551`)이
그것을 인정하며 *"질의/추론으로 계산"* 이라고 외부에 위임해 두었다.

### 2.3 A-Box 는 "요소로 표현"까지만 되어 있다

| 항목 | 실측 |
|---|---|
| ClaimFeature | **1,306,191건** / 문헌 40,187 (Claim 594,078 · 독립항 143,485) |
| 개념 접지율 | **33.1%** (432,305) · 접지에 쓰인 개념 **122종** |
| 질의측 / 후보측 접지율 | 48.6% (1,200 문헌) / 39.6% (2,479 문헌) |
| 비 KR/US 인용문헌 청구항 분해율 | **0%** (JP 0/186 · WO 0/24 · CN 0/6 · EP 0/1) |
| `PriorArtJudgment` | **635건** · `aboutClaim` 584 (**51건은 청구항 미접지**) |
| `scripts/eval_prior_art_realgt.py` 가 읽는 것 | 문서 단위 `title+abstract` — **ClaimFeature 를 한 번도 읽지 않는다** |

### 2.4 검증이 판단 능력을 잰 적이 없다

- CQ **31개 전부 ground BGP** — property path(`*` `+` `^` `/`) **0개**. `scripts/run_cq.py` 가
  *"추론은 쓰지 않는다"* 고 명시한다. 최종 실측 27/31이며 **선행기술 스위트(pa)는 4/8**.
- SHACL 33 NodeShape 는 구조 계약만 본다. 추론기는 파이프라인에 타깃이 없고, 있어도 **검출
  표면이 0**이다 — 모순을 만들 공리가 없다.
- 의미검색 측정: `scripts/eval_prior_art_realgt.py` 의 `onto` 랭커는 온톨로지를 **표면형 사전으로만**
  쓰는 문자열 매칭이며(그래프 순회·SPARQL·추론 없음), R@50 **0.1606** 대 tfidf **0.4330**.
  `eval_explanation_precision.py` 의 설명 coverage 는 0.6003 으로 문턱 0.70 미달이다.

### 2.5 업무 목적별 표본 — 신규성 축은 **결함이 아니라 문서화된 경계**다

| 원천 | 신규성(§29①) | 진보성(§29②) |
|---|---:|---:|
| **`PriorArtJudgment.onGround`** (인용문헌과 연결된 판단) | **9** | 626 |
| **`RejectionReason`** (근거만 · 인용 연결 없음) | **277** | 1,713 |
| 구조화 거절결정서 441건 | 14 | 400 |
| 의견제출통지서 원문 1,155건 | **259** | 1,108 |
| 인용 2건 이상 걸린 청구항 | 5 | **1,732** |

**핵심 대비.** 신규성 근거는 자원에 **`RejectionReason` 277건으로 존재한다.** 그런데 **선행기술
문헌과 연결된 판단에는 9건만 있다.**

**그 차이는 파이프라인 결함이 아니다.** `scripts/reextract_claim_judgments.py` 의 CR-004R 이
경계를 명시해 두었다 — *"판단(§29 근거·인용문헌 연결·PriorArtJudgment)은 거절결정서 표만 쓴다.
의견제출통지서에는 `cited_evidence_map` 이 없고, 새로 만드는 것은 범위 밖이다(검증 못 한 인용
해소를 쓰지 않는다)."* 즉 신규성 9건은 거절결정서(§29① 14문서) 유래로 **정상이고**, 277건은
통지서 유래인데 **인용문헌이 해소되지 않아 판단으로 승격되지 못한 것**이다.

**그러므로 이것은 결함 등재 대상이 아니라 범위 확장 작업이며, 그 작업이 곧 단계 2다.**

**회복 가능성은 실측으로 확인된다.** 통지서 1,155건: `인용발명 N` 82.7% · **문헌번호 75.2%** ·
공보 종류 91.9%. 그리고 통지서 보유 출원 **999건은 현 질의 1,000건과 999건이 겹친다** — 곧
**새 벤치마크를 만드는 것이 아니라 있는 질의가 근거와 함께 두꺼워진다.**

**엣지 단위 근거는 별도로 비어 있다** — 심사관 인용 엣지 2,534건의 `legal_basis` 는 전량 공란이다.
하위집단을 판단 단위로 가르려면 단계 2 에서 함께 채운다.

---

## 3. 아키텍처 판정 — 무엇이 OWL 로 되고 무엇이 안 되는가

세 업무 목적은 전부 두 층으로 분해된다.

- **원소층** — 질의 요소 `c_q` 하나가 문헌 개시 요소 `c_d` 하나로 충족되는가 (동일·상위·치환·주지관용)
- **집합층** — 그 판정을 요소 집합 전체에 정량화 (전부 포함 / 몇 % / 두 문헌 합집합이 덮는가)

> **OWL 이 잘 하는 것은 원소층이고, 못 하는 것은 집합층이다.**

### 3.1 신규성 — 포함은 "표현은 되지만 쓸 수 없다"

질의의 필수요소 집합이 고정되어 있으면 `owl:intersectionOf` + `owl:hasValue` 로 질의별 클래스를
만들어 분류할 수는 있다. 그러나 넷이 각각 독립적으로 치명적이다.

1. **질의마다 T-Box 가 바뀐다** — 자원이 질의에 의존하면 배포도 동결도 성립하지 않는다.
2. **불리언이지 등급이 아니다** — 20개 중 19개 충족이 0개 충족과 구별되지 않는다. 순위를 못 낸다.
3. **개방세계라 "신규하다"는 원리적으로 함의되지 않는다** — 얻을 수 있는 것은 *"신규성 없음"* 뿐.
4. **규모** — 추론기 비용이 문헌 4만 × 질의 1천에 성립하지 않는다.

**그리고 PLAN-001 §1.3 의 `sharesConceptWith` 계열 propertyChain 은 신규성 판정이 아니다.**
속성 체인은 존재 양화라 **개념 하나만 겹쳐도 간선이 생기고 스무 개가 겹쳐도 같은 간선 하나**다.
접지 개념 122종에 `material:sio2` 가 74k건인 현 자원에서 이것은 코퍼스를 거대한 동점 블록으로
묶는다. **이 넷은 판정 공리가 아니라 후보 생성 공리이며, 그 구분을 PLAN-001 에 명문화한다** —
구분하지 않으면 "공리 4개 신설"이 적격 충족으로 오독된다.

### 3.2 진보성 — OWL 밖이다. 우회로가 없다

"둘 이상 문헌의 개시 조합이 요소 집합을 덮는다"는 ① 개체 쌍 위의 술어 ② 두 개체의 합집합에 대한
양화 ③ 계수 를 요구하는데 **셋 다 OWL 문법에 없다.** `combinableWith` 는 아무것도 풀지 않는다 —
그것은 **결합 용이성**만 말하고 **결합 결과가 질의를 덮는지**는 말하지 않는다.

### 3.3 권고 아키텍처

> **OWL 은 원소층 매칭을 추론으로 닫고, SHACL 은 집합층 판정의 정본 정의를 적고,
> SPARQL 이 그것을 등급으로 계산한다.**

| 층 | 형식 | 역할 | 왜 |
|---|---|---|---|
| 원소층 | **OWL 2 RL** | `coveredBy` 로 동일·상위·치환·주지관용을 닫는다 | 추론이 실제로 값을 더하는 유일한 자리 |
| 집합층 정의 | **SHACL** | "필수요소가 전부 덮였는가"를 제약으로 | 폐쇄세계라 **부정을 표현할 수 있는 유일한 형식층** |
| 집합층 계산 | **SPARQL** | 커버율·조합 커버를 등급으로 | 순위를 내고 4만 규모에서 돈다 |

**이 배치에서 PLAN-001 §1.3 의 공리는 전부 살아남되 역할이 바뀐다.** `substitutableWith` ·
`broaderConcept` · `skos:exactMatch` 는 판정의 *결정자*가 아니라 **원소층 충족의 확장자**가 되고,
그 확장이 매칭을 몇 개 더 만들었는지는 **셀 수 있다**(§5 V1).

**함정 — `coveredBy` 를 전이로 선언하지 않는다.** 전이를 붙이면 "SiO₂↔SiON, SiON↔Si₃N₄" 두
판단에서 심사관이 한 적 없는 "SiO₂↔Si₃N₄" 가 자동 생성되고 `supportCount`/`counterCount`
provenance 를 우회한다. 대칭+전이는 122개 개념을 몇 개의 거대 동치류로 붕괴시킨다.
**확장 깊이는 의미론이 아니라 동결된 설정값으로 둔다**(`?`/`{0,1}`).

### 3.4 특허 행정 종속의 해소 — 삭제가 아니라 증거층으로 강등

```turtle
# ── 태스크층 (행정 어휘 0개) — PLAN-001 §1.2 재사용 ────────────────────
ont:TechnicalConcept   a owl:Class .
ont:StructuralElement  rdfs:subClassOf ont:TechnicalConcept .   # ← 축 부재 해소
ont:ProcessStep · ont:Material · ont:ProcessCondition ·
ont:Function · ont:TechnicalEffect · ont:TechnicalProblem       # 〃

ont:ClaimProfile  a owl:Class .        # 질의·문헌 공통 단위 — 특허가 아니어도 만들 수 있다
    # ont:essentialConcept → TechnicalConcept    (신규성 판단의 좌변)
    # ont:optionalConcept · ont:solvesProblem · ont:achievesEffect
ont:Disclosure    a owl:Class .        # 도달 목표 노드 (PLAN-001 §1.4-1)

# ── 신규 제안 (§2 3단계 별도 승인 대상) ────────────────────────────────
ont:coveredBy a owl:ObjectProperty .   # 원소층 충족 — 전이 선언하지 않는다(§3.3)
ont:broaderConcept · ont:substitutableWith · skos:exactMatch  rdfs:subPropertyOf ont:coveredBy .
ont:CombinedDisclosure / ont:combinesDisclosure .   # 진보성 조합 — 평가 산출물, 배포 자원 아님
ont:uncoveredConcept .                 # 판정의 잔여 = 설명가능성의 본체

# ── 증거층 (행정 어휘가 남는 유일한 자리) ──────────────────────────────
ont:PriorArtJudgment ont:assertedIn ont:ExaminationDocument ; ont:onGround ont:RejectionType .
ont:MinedAxiom prov:wasDerivedFrom <reason_id> .
```

**해소 여부는 기계로 검사한다 — 이 계획의 핵심 검사 규칙이다.**

> **태스크 질의의 `WHERE` 필수 부분에 `ont:Patent` · `ont:RejectedPatent` · `ont:RejectionType` ·
> `ont:PriorArtJudgment` · `ont:overPriorArt` 가 등장하면 실패.** 이들은 `OPTIONAL { }` 증거
> 블록에서만 허용한다.

이 검사가 통과하면 **연구노트로 만든 `ClaimProfile` 도 같은 질의로 검색된다** — 특허가 아직 없는
아이디어가 1급 질의가 된다. 지금 안 되는 이유가 정확히 CQ10 이 `?prior a ont:Patent` 로 시작하기
때문이며, **검사 규칙 하나가 "특허 종속" 지적 전체에 대한 검증 가능한 답이 된다.**

### 3.5 이식성 — 도메인 축과 관할 축 (2026-09-05 · 사용자 승인 · 안 C′-full)

§3.4 는 *"질의가 특허여야 하는가"* 를 풀었다. 그러나 **하류 논문이 실제로 받는 질문은 둘이 더
있고, 이 계획은 그 답을 갖고 있지 않았다** — 이식성은 PLAN-001~005 어디에도 목표로도 비목표로도
적혀 있지 않았다. **사용자 결정으로 목표에 넣는다.**

> **질문 A(도메인).** 반도체용 선행기술조사 온톨로지가 **바이오로 넘어가** 같은 업무를 수행할 때
> 인스턴스 추가 수준으로 적용되는가.
> **질문 B(관할).** KR §29 로 지은 것이 **US §102/§103 · EP Art.54/56** 으로 넘어가는가.

**두 질문의 실패 지점은 형태가 같다 — 슬롯이어야 할 자리를 열거로 굳힌 것이다.**

| 축 | 실패 지점 (워킹트리 실측) |
|---|---|
| 도메인 | `ont:featureConcept` range = 반도체 클래스 합집합 (`sdkb-patent.ttl:587-589`) |
| 관할 | `ont:noticeType`(*"의견제출통지서/거절결정서"* 리터럴) · `ont:examinationStatus` · `ont:groundClause`(*"조-항-호"*) (`sdkb-patent.ttl:431-451`) |

**선례가 저장소 안에 있다.** `ontology/sdkb-governance.ttl:15-18` — *"The Korean Industrial
Technology Protection Act is in a separate module … to keep jurisdictions decomposable."*
그리고 `ont:Rejection_Novelty a ont:RejectionType ; skos:notation "KIPO-29-1"` 은 **이미 개체
패턴**이라 관할이 클래스가 아니라 개체 속성에 들어 있다(`sdkb-patent.ttl:109-113`).
**발명하지 않고 같은 패턴을 선행기술 축에 적용한다.**

**설계·모듈 배치·`pa:underJurisdiction` 의 정본은 [PLAN-001 §1.10](PLAN-001-prior-art-axiom-resource-redesign.md)
이며, 여기서 되풀이하지 않는다.** 이 절이 지는 것은 **검증**(§5 V6)과 **비용의 비대칭**이다.

**비용은 두 축이 다르다 — 그 사실을 주장에 반영한다.**

| | 도메인 이식 (반도체 → 바이오) | 관할 이식 (KR → US) |
|---|---|---|
| L1 `pa:` core | 0줄 | 0줄 |
| L2 슬롯 바인딩 | 개념 스킴 교체 | 법조문·문서종 개체 추가 (**싸다**) |
| L3 채굴기 | **그대로 돈다** — KIPRIS 서식 동일 | **파서 전량 재작성** — USPTO 서식·인용 지시 문법이 다름 |
| L3 법리 | 동일 | **다르다** — KR 결합용이성 ≠ US §103 KSR/TSM |
| 원천 확보 | 단계 2 생성기 재실행 (거의 공짜) | 새 수집 파이프라인 |
| **판정 형태** | **실증**(V6a) | **설계 보증**(V6b) — 회수 성능은 재지 않는다 |

**그러므로 이 계획이 주장하는 명제는 *"인스턴스만 추가하면 된다"* 가 아니다.** 그 명제는
마쿠쉬 구조·서열목록·의약용도로 즉시 반례가 나온다. 주장하는 것은
**"L1 0줄 · L2 슬롯 교체 · L3 같은 생성기 재실행 · L4 인스턴스 추가"** 이며,
**이식되는 것은 공리가 아니라 공리를 만드는 방법이다.**

---

## 4. 재구성 범위 — 수직 관통 하나

**착수 단위는 수직 관통 하나다.** 업무 목적 하나를 골라 어휘 → 공리 → A-Box → 질의 → 검증까지
끝까지 관통한다. 도달률이 오르는지를 가장 빨리 알 수 있고, 오르지 않으면 적게 잃는다.

**첫 관통은 신규성이다.** 판단 형식이 *"요소 집합 ⊆ 단일 문헌 개시 집합"* 하나여서 신호가
깨끗하고, 진보성은 그 위에 합집합 커버와 결합 용이성을 얹은 상위집합이다. **다만 §2.5 대로
신규성 표본이 소실되어 있으므로, 관통의 첫 작업은 그 축을 되살리는 것이다.**

| 층 | 작업 | 근거 |
|---|---|---|
| **표본** | **통지서 인용 해소** — CR-004R 경계를 넘어 `RejectionReason` 277건을 인용문헌과 연결된 판단으로 승격하고, 근거를 엣지 단위로 부착 | §2.5 |
| **어휘** | `TechnicalConcept` / `StructuralElement` 축 신설 — 보류된 구조요소 15개를 받는다 | §2.1 |
| **공리** | `coveredBy` + `broaderConcept`(전이) · `substitutableWith`(대칭) · 역관계. **`combinableWith` 는 진보성 관통으로 미룬다** | §3.3 |
| **공리 원천** | 통지서 채굴 · `MinedAxiom` + `axiomConfidence` + `supportCount`/`counterCount` + `prov:wasDerivedFrom` 필수 | PLAN-001 §1.2(c) · PLAN-002 |
| **A-Box** | `Disclosure` 실체화 · 개념 접지율 33.1% 개선 · 인용측 분해 결손 처리 방침 | §2.3 |
| **질의** | §3.4 검사 규칙을 통과하는 신규성 SPARQL 1건 + CQ 스위트 편입 | §3.4 |

**진보성은 두 번째 관통이다** — 정답이 이미 1,732 청구항 규모로 있어 A-Box 비용이 낮다. 후보
상한 안에서 쌍을 실체화하고(top-K → 질의당 K(K−1)/2) SPARQL 로 합집합 커버를 계산한다. 실체화된
`CombinedDisclosure` 는 질의 의존이므로 **배포 자원 번들에 넣지 않는다.**

**FTO 는 범위 밖이며, 그 이유를 명세로 싣는다.** 판단의 방향이 뒤집힌다 — 선행기술조사는
*"내 요소가 선행문헌에 있는가"*(질의→문헌)이고 침해는 *"내 실시가 등록청구항의 모든 구성요소를
포함하는가"*(권리→실시형태)다. 그리고 축이 없다 — `legalStatus` · `grantDate` · `expiryDate` ·
`jurisdiction` 이 T-Box 에 **한 건도 선언돼 있지 않고**, 코퍼스는 거절출원+인용문헌이라 대부분
권리가 아니며, 외부 정답은 라이선스·소송 기록이지 심사관 인용이 아니다.
`queries/cq/CQ27_fto_claim_readiness.rq` 가 재는 것은 **준비도이지 FTO 판정이 아니다.**

---

## 5. 검증 설계

재는 것은 *"추론기가 판단을 출력하는가"* 가 아니라 **"공리 기반 요소 확장이 판단을 개선하는가"**
이다. 그러므로 모든 층이 **확장 전 / 확장 후**의 쌍으로 측정된다.

### V1 구조 — 공리 단위 절제로 "쓰이지 않는 공리"를 탐지

3열 대조표만으로는 부족하다. 표는 손으로 채우면 통과한다. **기계 절차를 붙인다.**

```
각 R-Box 공리 a 에 대하여:
  ① a 를 뺀 자원 번들 O∖a 를 만든다
  ② CQ 스위트 전량 실행       → 응답 행 수 벡터
  ③ 원소층 확장 재구체화       → coveredBy 매칭 수
  ④ 태스크 질의 실행          → 커버율 분포
  a 가 "소비된다" ⟺ ②③④ 중 하나가 변한다.  변하지 않으면 자원에 넣지 않는다.
```

**기준선이 곧 결과다.** 현 R-Box 8건 전량에 이 절제를 돌리면 *"공리 8건 중 태스크에 소비되는
것은 n건"* 이 나오고, 그것이 지금 *"8건이 있다"* 만 보고되는 자리를 대체한다.

### V2 도달 — `SemanticPathRecall`, 단 **특이도를 동반한다**

```
질의 단위 q = (질의 프로파일, 독립 요소집합)
마스킹 M   = { hasPriorArt, hasPriorArtExaminer, hasPriorArtApplicant, overPriorArt,
               overDisclosure, overlappingFeature, hasJudgment, aboutClaim, NoveltyScore }
Reach(q)   = G∖M 에서 확장 경로를 깊이 L(동결) 이하로 따라 도달한 문헌
SemanticPathRecall@S = |{ q : 목표∩Reach(q) ≠ ∅  ∧  |Reach(q)| ≤ S }| / |Q|
```

**목표 노드는 두 수준으로 나눈다.** PLAN-001 정본인 `Disclosure` 는 **클래스 선언조차 없고
인스턴스 0건이라 오늘 잴 수 없다.** 그러므로 기준선은 **인용문헌 노드**(퇴화형)로 재고 그 사실을
함께 적으며, 정본 수준은 A-Box 실체화 이후에 잰다. **PLAN-003 진입 조건에 이 제약을 명시한다** —
지금은 오늘 잴 수 있는 것처럼 읽힌다.

**특이도가 없으면 이 지표에는 퇴화 최적해가 있다.** `sharesConceptWith` 는 존재 양화이고
`material:sio2` 가 74k건이므로 **흔한 개념 하나를 모든 문헌에 붙이면 도달률이 1.0 에 접근한다.**
따라서 진입 조건은 절대값 하나가 아니라 셋이다: (i) 기준선 대비 유의 상승 (ii) 절대 하한 τ 통과
(iii) **`|Reach(q)|` 중앙값이 기준선 대비 증가하지 않음.** (iii) 이 없으면 개념을 늘리는 것만으로
항상 통과한다.

### V3 업무 목적 — 신규성은 표본이 회복된 뒤에만 정량 판정한다

| | 지금 | 처리 |
|---|---|---|
| **진보성** | 청구항 2,305 · 2문헌 이상 1,732 | **주 지표.** `PairCoverage@K` · `ΔCoverage`(조합이 단일 최대를 실제로 넘는가) · `combinableWith` 지지 유무 층화 |
| **신규성** | 판단 9 · 특허 5 · 청구항 8 | **정량 판정 금지.** ① 형식 성립성(질의가 비공백 응답) ② 프록시: 인용 1건만 걸린 청구항 573개 — **프록시임을 명기** ③ 사람 표본 n≈50 · 2인 독립 코딩 · κ 동반 |

**§4 의 표본 회복이 성공하면 신규성이 문서 254건 규모로 올라오고, 그때 비로소 정식 하위집단이
성립한다.** 실패하면 *"신규성 하위집단을 이 자원이 지지하지 않는다"* 를 **결과로 보고한다.**

### V4 질의 비종속성 — "특허가 아니어도 되는가"를 실제로 잰다

§3.4 검사 규칙은 **질의문이 행정 어휘를 쓰지 않는다**는 것만 보장한다. **실제로 연구노트에서도
작동하는가**는 별도로 재야 한다.

1. **표현 강건성(정답 불변)** — 같은 질의의 독립항 원문을 **요약문·의역문**으로 바꾸고 회수를
   비교한다. 정답지가 그대로이므로 새 정답 없이 잴 수 있다. **프록시이며 그렇게 적는다** —
   요약문은 연구노트가 아니다.
2. **실제 노트 소표본** — 도메인 연구노트·과제제안서 요약 n≈30 을 수집해 프로파일을 만들고
   회수 결과를 **2인 독립 판정**한다(κ·합의율 동반). 표본이 작으므로 **정성 보고**로 싣는다.
3. **판정** — 1에서 회수 저하가 크면 도구는 여전히 청구항 문체에 의존하는 것이고, 그 사실이
   결과다.

> **결정 (2026-09-05 · 사용자 승인) — V4-1 의 주 경로는 LLM 의역이다.**
> 대안으로 **초록(사람 작성)** 을 주 경로로 쓰는 안이 제시됐고(초록 1,000/1,000 보유 · 청구항1
> 과의 자카드 중앙값 0.500 · 30.3%가 0.3 미만), 사용자가 **LLM 의역**을 택했다. 초록 안은
> 폐기하지 않고 **대조 축으로 남긴다** — 비용이 0 이고, LLM 의역의 수를 해석할 눈금이 된다.
>
> **그러므로 타당성 조건 하나가 설계에 못박힌다 — 의역 생성 모델과 청구항 분해 모델은 달라야
> 한다.** 같으면 재는 것이 표현 강건성이 아니라 **그 모델의 자기일관성**이다.
>
> **그리고 §1-5(개인정보)가 걸린다.** 청구항·초록에는 성명이 없으나 의견제출통지서·거절결정서
> 에는 출원인·대리인·심사관 성명이 있다. **V4 는 청구항만 쓰므로 해당 없고**, 단계 2·PLAN-002
> 채굴에 LLM 을 쓰려면 `scripts/scrub_rejection_excerpts.py` 선행이 **규약상 필수**다.
>
> **재현성.** LLM 출력은 재현이 보장되지 않는다. 그러므로 동결하는 것은 생성기가 아니라
> **생성된 질의 세트**이며, sha256 과 함께 커밋한다(§2 3단계 결정성 요구).

### V5 기준선을 먼저 박는다

**재구성 전에 V1–V4 를 현 자원에서 1회 산출하고 커밋한다.** 목표 수치는 결과를 보기 전에
동결한다(§1.4 · §2 3단계).

- **V2 는 지금까지 한 번도 산출된 적이 없다** — 기준선 산출 자체가 이 계획의 첫 실측 산출물이다.
- **V3 기준선은 포화를 드러낼 것으로 예상된다** — 개념 122종·접지 33.1% 에서는 질의당 필수요소가
  1~2개뿐이라 커버율이 0 또는 1로 이산 포화한다. **그 포화가 재구성 필요성의 가장 직접적인
  증거이며, 동시에 개선을 잴 유일한 눈금이다.**
- **대조군을 반드시 둔다** — `eval_prior_art_realgt.py` 의 tfidf(R@50 0.4330)가 온톨로지가 넘어야
  할 선이다. 넘지 못하면 넘지 못한 대로 보고한다.
  > **결정 (2026-09-05 · 사용자 승인).** 기존 `data/reports/prior_art_realgt_report.json` 값을
  > **그대로 쓰고 재실행하지 않는다**(n=974 · tfidf R@50 0.4330 · onto 0.1606 · hybrid 0.4234).
  >
  > **단, 단일 수로 읽지 않는다 — 같은 리포트가 언어 층화를 담고 있다.**
  > tfidf R@50 은 **KR 0.6708 · FOREIGN 0.0074** 이고, hybrid 는 FOREIGN 을 0.0599 로 올리는
  > 대신 KR 을 0.6272 로 내린다. **넘어야 할 선이 층마다 다르며 FOREIGN 층에서 tfidf 는
  > 사실상 0 이다.** 층화를 빼면 *"온톨로지가 tfidf 를 못 넘었다"* 와 *"tfidf 가 못 하는 층에서
  > 이겼다"* 가 같은 수 하나에 뭉개진다(PLAN-003 §2.5 T2 하위집단 '언어').
  > 이 값들은 `data/reports/priorart_baseline.json` 의 `control_group` 에 기록했다.
- **V6 은 기준선을 갖지 않는 유일한 검증이다** — 재구성 전에는 `pa:` core 자체가 없으므로 잴
  대상이 없다. V6 의 목표는 기준선 대비가 아니라 **절대 불변식**(변경 0줄 · IRI 0건)이며,
  §9-3 은 V6 에 대해 그렇게 읽는다.

### V6 이식성 — 도메인은 실증하고, 관할은 설계로 보증한다 (2026-09-05 신설)

**V4 가 *"특허가 아니어도 되는가"* 를 쟀다면 V6 은 *"반도체가 아니어도, 한국이 아니어도
되는가"* 를 잰다.** 근거와 비용 비대칭은 §3.5, 설계 정본은 PLAN-001 §1.10.

**(a) CI 불변식 — 가장 싸고, 이것이 주장의 뼈대다.**

> `sdkb-priorart-core.ttl` 안에 **도메인 이름공간(`ont:`·`semi:`) 또는 관할 이름공간(`kr:`·`us:`)
> 의 IRI 가 한 건이라도** 등장하면 **실패**.
> 역방향 — 태스크 질의·집합층 SHACL 정의가 `semi:`·`kr:` 를 `WHERE` 필수부에 쓰면 **실패**.

§3.4 규칙과 **같은 CI 러너**에 붙인다. 이 둘이 통과하면 *"이식 시 L1 변경 0줄"* 이 주장이 아니라
**기계 보증**이 된다.

**(b) V6a · 도메인 실물 이식 — 실증.**

바이오 소코호트 **N≈30–50**(A61K/C12N). **원천이 같아서 싸다** — KIPRIS 는 전 분야를 담고
의견제출통지서 서식·§29 조항 인용·*"인용발명 N"* 지시 문법이 동일하므로, **단계 2 의 통지서 인용
해소 생성기가 그대로 돈다.**

| 산출 | 무엇을 말하는가 |
|---|---|
| **L1 변경 라인수** | **0 이면 이식성 증명.** 0 이 아니면 **어디가 도메인 종속이었는지가 그 자체로 기여** |
| L2 신규 개념 수 · 접지율 | 개념층 교체 비용 |
| L3 재실행 수율(`P_uniq`·`Q_cov`) | 채굴 **방법**의 이식성 — PLAN-002 와 같은 눈금 |
| 회수 지표 대 tfidf | 도구가 실제로 작동하는가 |

**실패할 것으로 미리 적어 둔다 (결과를 보고 고르지 않기 위해 · §1-2).**

1. `featureRole` 열거 `{Means, Structure, Step, Material, Condition, Function, Effect}` 가
   **바이오를 못 담는다** — 서열·투여용법·적응증·마쿠쉬 선택지가 갈 자리가 없고, `Condition`
   으로 밀면 의미가 뭉개진다(§1-3 위반). **이것이 나오면 L1 이 오염돼 있었다는 증거이며,
   열거 자체를 슬롯으로 내려야 한다.**
2. 청구항 분해 파이프라인의 **문체 종속** — V4 와 같은 축이므로 두 검증이 계측기를 공유한다.
3. 바이오 법리 차이(선택발명·수치한정 임계적 의의·의약용도)로 `substitutableWith` 판단 분포가
   다름 — **L3 은 원래 이식 대상이 아니므로 예상된 결과이지 실패가 아니다.**

**(c) V6b · 관할 종이 이식(paper port) — 설계 보증.**

`sdkb-priorart-us.ttl` 바인딩 모듈을 **실제로 써서** US §102/§103 을 `pa:LegalGround` 개체로,
non-final/final Office Action 을 `pa:documentRole` 개체로 넣고 **core 변경 0줄 · 태스크 질의와
집합층 SHACL 무수정 통과**를 확인한다. **A-Box 없이 성립한다** — 파일과 질의만으로 증명되기
때문이다.

> **US 문헌 회수 성능은 재지 않으며, 그렇게 적는다**(§1-4). V6b 가 답하는 것은 *"슬롯이 실제로
> 작동하는가"* 이지 *"US 에서 성능이 나오는가"* 가 아니다. 후자는 §7 비목표다.

**(d) 관할 혼합 추론 금지의 검사.** `pa:underJurisdiction` 이 없는 `MinedAxiom` 은 SHACL 위반이며,
한 추론 실행이 서로 다른 관할의 공리를 함께 쓰면 실패한다(PLAN-001 §1.10(e)).

---

## 6. 순서와 정지 게이트

| # | 작업 | 게이트 | 산출 |
|---|---|---|---|
| 0 | 본 진단·아키텍처 판정을 **PLAN-001 에 편입** — chain 은 후보생성기이지 판정이 아님 · `coveredBy` 전이 금지 · OWL/SHACL/SPARQL 배치 · §3.4 검사 규칙 | **§2 1단계 🛑** | PLAN-001 개정 |
| 0′ | **PLAN-004 §3 종료조건 개정** — "공리↔소비자 대조"를 V1 절제 절차로 조작화 | 0 에 포함 | PLAN-004 개정 |
| 1 | **V1–V4 기준선 측정** (읽기 전용) | **§2 2단계 🛑** | 계수 리포트 |
| 2 | **통지서 인용 해소** + 근거를 **엣지·판단 단위로 부착** — CR-004R 이 그은 경계를 넘는 작업 | **§2 3단계 🛑** → 4·5단계 | 프로파일 의무 동반 |
| 3 | PLAN-002 `Prec` 산출 → 착수 판정 확정 | PLAN-004 단계 0 🛑 | κ 동반 |
| 4 | **T-Box·R-Box 신설**(`sdkb-claim-semantics.ttl`) — 기존 어휘 불변 · `owl:imports` | **§2 3단계 별도 승인 🛑** | 생성기 경유(§1-1) |
| 5 | A-Box — `Disclosure` 실체화 · 접지율 개선 | **§2 3단계 🛑** | 미매핑률 리포트 |
| 6 | **V1 재실행 → 소비되지 않는 공리 삭제** | §2 5단계 | 공리↔소비자 대조표 |
| 7 | **V2–V4 재측정 · 동결 목표와 대조** | §2 5단계 🛑 | 판정 |
| **8** | **V6b · US 종이 이식** — 바인딩 모듈 + core 변경 0줄 확인 | §2 5단계 | 설계 보증 리포트 |
| **9** | **V6a · 바이오 실물 이식** (N≈30–50) | §2 5단계 🛑 | 이식 리포트 |

**8 은 9 보다 싸므로 먼저 해도 된다** — A-Box 가 필요 없고 파일·질의만으로 성립한다(§5 V6b).
**8·9 의 CI 불변식(§5 V6(a))은 4 와 같은 커밋에서 세운다** — 나중에 붙이면 이미 오염된 core 를
통과시키게 된다.

**3은 1·2와 병렬 가능하다.** `Prec` 은 4(공리 신설)의 선행 조건이지 진단·기준선의 선행 조건이
아니다 — PLAN-004 가 *"병렬 가능한 것은 없다"* 고 적은 것은 이 진단 이전의 판단이다.

**7에서 미달하면 멈춘다.** 그때의 보고는 실패가 아니라 *"이 업무 목적을 이 자원이 지지하지
않는다"* 는 측정 결과다.

---

## 7. 비목표

1. **하류 저장소의 코드·판정·수치·사전등록을 건드리지 않는다.** 하류가 얼린 스냅샷을 가져가는
   것은 하류의 결정이며 이 계획의 임계경로가 아니다.
2. **기존 어휘를 제자리에서 바꾸지 않는다** — 새 모듈을 `owl:imports` 로 더한다. §0 소비자
   계약은 구조로 지킨다.
3. **FTO 를 이번에 잰다고 주장하지 않는다** — 결손 명세로만 싣는다.
4. **전문가 매칭·기술예측 뷰를 이번에 채우지 않는다** — 선행기술 뷰 한정.
5. **합성 A-Box 에서 정답을 파생하지 않는다** — 온톨로지가 만든 사례를 온톨로지 경로로 복구하는
   순환이다.
6. **표를 채우려고 공리를 만들지 않는다** — V1 절제에서 소비되지 않는 공리는 넣지 않는다.
7. **TTL 을 손으로 고치지 않는다**(§1-1) — 전부 생성기 경유.
8. **US·EP 의 검색 성능을 잰다고 주장하지 않는다** (2026-09-05 · 안 C′-full). 관할 축은
   **종이 이식(V6b)과 CI 불변식으로 설계 보증까지**이며, US 원천 수집 파이프라인과 회수 실측은
   **범위 밖**이다 — §3.5 의 비용 비대칭이 그 이유다.
9. **관할이 다른 공리를 한 추론에 섞지 않는다** — `pa:underJurisdiction` 이 그 금지의 기계적
   근거다(PLAN-001 §1.10(e)).

---

## 8. 산출물과 그 자리

| 단계 | 산출물 | 자리 |
|---|---|---|
| 0 | 이 계획서 | `01.code_spec/plans/PLAN-005-*.md` |
| 0 | 진단 보고서(§2 전문 + 재현 명령) | `01.code_spec/reports/PLAN-005-diagnosis.md` |
| 1 | V1–V4 기준선 | `data/reports/` |
| 2 | 통지서 인용 해소 · 근거 부착 | `data/patents/` |
| 4 | `sdkb-claim-semantics.ttl` | `ontology/` — 생성기 경유 |
| 4 | 신규성 태스크 질의 | `queries/cq/` — §3.4 검사 규칙 통과 필수 |
| 4 | `sdkb-priorart-core.ttl` · `-semi.ttl` · `-kr.ttl` | `ontology/` — 생성기 경유 |
| 6·7 | 공리↔소비자 대조표 · 최종 판정 | `01.code_spec/reports/` |
| 8 | `sdkb-priorart-us.ttl` · 종이 이식 리포트 | `ontology/` · `01.code_spec/reports/` |
| 9 | 바이오 이식 리포트 (L1 변경 라인수 · L2·L3 수율) | `01.code_spec/reports/` |

---

## 9. 완료 기준 (이 계획 자신)

1. §2 의 모든 수치가 실행된 명령의 출력으로 재현된다(명령·경로 병기 — `PLAN-005-diagnosis.md`).
2. §4 의 각 재구성 항목이 §2 의 어느 진단 항목에 대응하는지 표로 확인된다 — 대응 없는 항목은
   넣지 않는다.
3. §5 의 각 검증이 **기준선**과 **결과를 보기 전에 동결한 목표**를 각각 갖는다.
4. **§3.4 검사 규칙이 CI 에서 돈다** — 태스크 질의가 행정 어휘를 필수부에 쓰면 실패한다.
5. **V1 절제에서 소비 경로가 확인되지 않은 공리가 자원에 0건이다.**
6. **`sdkb-priorart-core.ttl` 에 도메인·관할 IRI 가 0건이고 그것을 CI 가 검사한다**(§5 V6(a)).
7. **`pa:underJurisdiction` 이 없는 `MinedAxiom` 이 0건이다** — SHACL 로 강제한다.
8. **V6a·V6b 가 각각 산출되고, L1 변경 라인수가 보고된다** — 0 이 아니면 그 값과 원인을 적는다.

---

## 10. 단계 2-A 설계 — 통지서에서 `legal_basis` 를 엣지에 부착한다 (§2 3단계 🛑)

> **지위: 3단계 설계 · 승인 대기 🛑.** 근거는 [2단계 분석 리포트](../reports/PLAN-005-stage2-notice-analysis.md).
> **단계 2 를 셋으로 갈랐고(A·B·C) 이 절은 A 만 설계한다** — A 가 B·C 를 기다릴 이유가 없고,
> C 가 어려워서 A 가 지연되는 것이 이 저장소에서 반복된 실패 형태다.

### 10.1 왜 A 를 먼저 하는가

심사관 엣지 **2,534건의 `legal_basis` 가 전량 공란**이라 **거절근거별 하위집단 분석 자체가
막혀 있다**(PLAN-003 §2.5 T2 가 요구하는 축). 채워진 656건은 전부 `source_type=evidence_v2`
(거절결정서 유래)다.

    source_type   건수    legal_basis 채움
    all           3,485          0
    evidence         17          0
    evidence_v2     656        656
    examiner      2,534          0

원천 보유율이 **99.4%**(법조항×청구항 표)·**96.3%**(번호 절)로 셋 중 가장 확실하다.

### 10.2 재사용한다 — 새로 만들지 않는다

**`legal_basis` 파이프라인은 거절결정서용으로 이미 끝까지 존재한다.**

    build_rejection_decisions.py   PDF→OCR→structured (cited_evidence_map · legal_bases)
      _CITED_LINE_RX               "인용발명 N : 공개특허공보 제10-2015-0109288호"
      _normalize_cited_id()        → "KR-P-1020150109288"  (edges.cited_doc_id 와 같은 형식)
    apply_phase_c_to_canonical.py  → ground_truth_evidence_v2 (legal_basis 부여)
    build_abox_claim_features.py:428  source_type=="evidence_v2" 를 읽어 RejectionType 매핑

**A 는 같은 기계를 통지서에 적용하는 일이다.** `_normalize_cited_id()` 를 **재구현하지 않고
import 한다** — 두 벌이 되면 갈린다.

### 10.3 무엇을 만드는가

| | |
|---|---|
| 신설 | `scripts/build_notice_evidence.py` |
| 입력 | `data/sources/opinion_notices/txt/*.txt` (1,155) · `_index.json` |
| 출력 1 | `data/sources/opinion_notices/structured/{출원번호}.json` — 결정서와 **대칭 스키마** |
| 출력 2 | `data/patents/prior_art_edges.parquet` 의 **`legal_basis` 컬럼만 갱신** |
| 리포트 | `data/reports/notice_evidence_report.json` (프로파일 의무 §5) |

**OCR 은 하지 않는다** — 통지서는 txt 가 이미 있다. `build_rejection_decisions.py` 의 다운로드·
OCR 경로는 타지 않는다.

### 10.4 추출 알고리즘 — 결정적이다

```
문서 t 에 대하여
  ① 절 분할     `^\s*(\d+)\.\s*이\s*출원`  → 절 목록 (96.3% 보유 · 문서당 1~6)
  ② 절 → 법조항  절 본문의 `제29조제1항|제29조제2항|제42조` 재진술 (절 2,591개에서 확인)
  ③ 절 → 인용발명 `_CITED_LINE_RX` 로 `인용발명 N : <표기>` 수집 (절 1,024개에서 확인)
  ④ 표기 → 식별자 `_normalize_cited_id()` (import 재사용)
  ⑤ 절 → 청구항  `_CLAIM_FOCUS_RX` 로 대상 청구항 범위
  ⑥ 부착        (출원, 식별자) 로 edges 를 찾아 legal_basis 를 쓴다
```

**난수·시각·해시 순서에 의존하지 않는다.** 같은 원천 → 같은 산출. LLM 을 쓰지 않으므로
§1-5 개인정보 제약(통지서의 심사관 실명)에 걸리지 않는다 — **원문은 저장소 밖으로 나가지 않는다.**

### 10.5 하류를 깨지 않는 방식 — 이것이 이 설계의 핵심 선택이다

**엣지에 새 `source_type` 을 만들지 않는다.** `build_abox_claim_features.py:428` 이
`source_type == "evidence_v2"` 만 읽으므로, 새 값을 넣으면 **ABox 가 달라지고 §0 소비자가
움직인다.** 대신 **기존 `examiner` 엣지의 `legal_basis` 컬럼만 채운다.**

| 대상 | 바뀌는가 |
|---|---|
| `prior_art_edges.parquet` 의 `legal_basis` (examiner 행) | **채워진다** ← 이 작업의 전부 |
| `source_type` · 엣지 집합 · 엣지 수 | **불변** |
| TBox · shape · IRI 규칙 | **불변** |
| `sdkb-abox-*.ttl` | **불변** (생성기가 examiner 엣지의 legal_basis 를 읽지 않는다) |

**그러므로 이 작업은 그래프를 바꾸지 않는다.** 하류 스냅샷은 영향받지 않고, 바뀌는 것은
**하위집단 분석에 쓸 수 있는 열 하나**다.

### 10.6 성공 기준 — 결과를 보기 전에 동결한다

| | 판정량 | 문턱 |
|---|---|---|
| 주-1 | examiner 엣지 중 `legal_basis` 채움 | **≥ 1,200 / 2,534 (47%)** — 인용발명 정의줄 보유 72.2% 에 절 단위 해소 손실을 감안한 유도값 |
| 주-2 | 부착된 값이 결정서 유래 656건과 **교차 검증**되는 비율 | 같은 (target, cited) 쌍이 양쪽에 있을 때 **일치율 ≥ 0.90** |
| 보조 | §29① 부착 엣지 수 | **≥ 100** (현재 10) |
| 품질 | 사람 표본 30건 정합 | 통지서 원문과 대조해 **≥ 0.90** |

**주-2 가 이 설계의 진짜 검증이다** — 결정서에서 이미 확인된 값과 통지서에서 새로 뽑은 값이
어긋나면 추출이 틀린 것이다. **이 대조가 가능한 것은 두 문서를 모두 가진 출원이 있기 때문이다.**

### 10.7 비목표

1. **인용문헌을 새로 해소하지 않는다** — 기존 엣지에 없는 (target, cited) 쌍은 만들지 않는다.
   그것은 **작업 B** 이며 별도 승인 대상이다.
2. **구성 대비표를 건드리지 않는다** — 작업 C.
3. **`PriorArtJudgment` 를 새로 만들지 않는다** — 신규성 축 9→N 승격은 B 다.
4. **TTL 을 손으로 고치지 않는다**(§1-1).

### 10.8 누출 규율

`legal_basis` 는 **정답 간선의 속성**이다. 검색 입력으로 새어 들어가면 안 되므로 §1.6-4
마스킹 목록에 준해 다룬다 — **평가 하위집단 분해에만 쓰고 랭커 입력에 넣지 않는다.**
이 사실을 리포트와 컬럼 주석에 함께 적는다.

---

### 10.9 개정 — 다중 근거를 보존한다 (2026-09-06 · 5단계 미달 후 3단계 복귀 · 사용자 승인 ③)

**첫 구현의 판정은 *주-2 미달*(0.8807 < 0.90)이었고, 원인은 추출이 아니라 §10.5 의 tie-break
규칙이었다.** 불일치 76건 중 **69건이 결정서 §29② → 통지서 §29①** 방향이고, **48건(63%)은
통지서가 결정서 값도 함께 담고 있었다** — 즉 근거가 둘인 쌍을 하나로 뭉개면서 갈렸다.
실제로 한 (출원, 인용) 쌍에 근거가 둘인 경우가 **53건** 있다.

**그러므로 뭉개기를 없앤다.** §10.5 의 *"§29① 우선"* 규칙은 **폐기**한다.

| | 개정 전 | 개정 후 |
|---|---|---|
| 엣지 부착 | `legal_basis` 단일값 (tie-break) | **새 컬럼 `legal_bases`** — `|` 결합 다중값 |
| 기존 `legal_basis` | examiner 행을 채움 | **건드리지 않는다** |
| 정본 | `notice_legal_basis.parquet` | 동일 (이미 (출원,인용,근거) 한 행씩이라 다중값 보존) |

**기존 `legal_basis` 를 재사용하지 않는 이유는 §1-3 이다** — 그 컬럼은 `evidence_v2` 의
단일값 계약으로 이미 소비되고 있고(`build_abox_claim_features.py:428`), 같은 이름에 두 가지
값 모양을 두면 **이름이 의미와 달라진다.** 새 의미에는 새 이름을 준다.

**하류 확인.** `tests/test_patents.py` 는 `source_type` 을 **부분집합**으로만 검사하고 엣지의
컬럼 집합을 고정하지 않는다. 새 `source_type` 을 만들지 않으므로 이 검사는 그대로 통과한다.
ABox 생성기는 `evidence_v2` 만 읽으므로 **TBox·shape·IRI·TTL 은 여전히 불변**이다.

### 10.10 검증 문턱 재설정 — 그리고 정직하게 밝힐 것

**`containment` 0.9560 을 게이트로 쓰지 않는다.** 그 수는 **이미 본 뒤**이므로, 지금 그것을
"동결된 문턱"이라 부르면 결과를 보고 문턱을 고르는 것이 된다(§1-2). §10.6 이 "일치율"의
비교 단위를 명시하지 않은 것은 **설계의 흠**이며, 흠을 사후에 유리한 쪽으로 메우지 않는다.

| | 판정량 | 지위 |
|---|---|---|
| 주-1 | 채움 ≥ 1,200 | **유지** — 개정과 무관 |
| 보조 | §29① 부착 ≥ 100 | **유지** |
| ~~주-2~~ | ~~교차 검증 일치율 ≥ 0.90~~ | **게이트에서 내린다.** 단일값 비교는 폐기된 tie-break 를 재는 것이고, 집합 비교는 이미 본 수다. **두 수(0.8807 / 0.9560)를 서술 통계로 계속 보고한다** |
| **주-2′** | **사람 표본 30건 원문 대조 ≥ 0.90** | **새 주 문턱.** **아직 한 번도 측정하지 않은 지표**이므로 지금 동결하는 것이 사후 조정이 아니다 |

**주-2′ 가 A 의 최종 게이트다.** 표본은 통지서 원문과 부착값을 사람이 대조하며, 시드 고정으로
추출한다. 이것이 산출되기 전에는 **A 를 완료로 보고하지 않는다.**


---

## 11. 단계 2-C 설계 — 구성 대비표에서 **구조 키**를 채굴한다 (§2 3단계 🛑 · 미승인)

관찰 정본: [PLAN-005-stage2c-table-analysis.md](../reports/PLAN-005-stage2c-table-analysis.md).
모든 수치는 그 문서의 재현 명령 출력이다.

### 11.0 먼저 밝힐 것 — 규모가 §4 의 기대보다 작다

§4 는 C 를 *"`coveredBy`·`uncoveredConcept` 의 직접 증거"* 라 적었고
[단계 2 분석](../reports/PLAN-005-stage2-notice-analysis.md) §4 는 원천 보유율을
17.4%(단순 파싱 42%)로 봤다. **재관찰 결과 신뢰 가능한 축은 그보다 작다.**

| | 값 |
|---|---:|
| 청구항에 접지되는 (출원, 청구항, 구성, 판정) 키 | **305행 · 26출원** |
| 청구항 키가 없는 행(판정 라벨만) | 611행 · 151출원 |
| 참고 — PLAN-002 문단 채굴 `P_uniq` | 3,044 |

**작아진 것은 자료가 아니라 주장이다.** 원 분석 §3.1 이 *"삼중항이 표로 있다"* 고 적은 것은
**PDF 원본**에 대해 참이고, 우리가 가진 **OCR 텍스트**에 대해서는 참이 아니다 — 열이 무너져
(본원 구성, 인용 개시) 쌍이 조각으로만 남는다(관찰 §3.2). 조각을 개시 텍스트로 승격하는 것은
§1-3 위반이므로, **쓸 수 있는 것은 구조 키와 판정 라벨뿐**이다.

> **그러므로 이 설계는 "적게, 정확하게"를 택한다.** 305행은 심사관이 **직접 짝지은** 판정이라
> 문단 채굴과 정밀도의 층이 다르다. 그러나 **양으로 PLAN-002 를 대체하지 못한다.**
> 이 사실을 승인 전에 밝히는 이유는 §1-4 다 — 규모를 낙관해 두고 나중에 조정하지 않는다.

### 11.1 무엇을 만드는가

**산출은 parquet 하나이고, 이번에 TTL·TBox·shape 을 만들지 않는다.**

```
data/patents/notice_element_judgments.parquet
  application_number : str   출원번호 (통지서 파일명 앞부분 — 파일명 ≠ 출원번호, 2-A 교정 6)
  source_file        : str   원천 통지서 파일명 (발송번호 포함 · 한 출원에 여러 통지서)
  table_index        : int   문서 내 <표 N> 의 N
  element_group      : int   구성 <element_group>-<element_no> 의 앞 숫자 = **표 단위 구성 그룹 번호**
  element_no         : int   같은 표기의 뒷 숫자 = 그 그룹 안의 구성요소 번호
  caption_claim_no   : Int64 표 캡션이 말하는 **청구항 번호**. 결측 가능(38/305)
  judgment_raw       : str   원문 판정 어휘 그대로 (실질적 동일 · 차이 · 대응 · …)
  judgment           : str   정규화 라벨 {Identical, Different, Similar, Corresponding, WellKnown, DesignChange}
```

> **§11.1-a 개정 — 앞 숫자는 청구항 번호가 아니다 (2026-09-06 · 4단계 중 3단계 복귀 ·
> 사용자 승인).**
>
> 초판은 `구성 N-M` 의 앞 숫자를 `claim_no` 라 불렀다. **틀렸다.** 실물에서 그것은 **표
> 단위의 구성 그룹 번호**이고, 청구항 번호는 표 캡션("청구항 3 발명과 인용발명1, 2를
> 비교해 보면 아래 표2와 같습니다")에 있다. `1020127009380` 의 `<표2>` 는 `구성 2-1…2-6`
> 을 담는데 그것은 전부 **청구항 3** 의 구성이다.
>
> | 전수 대조 (캡션 = 앞 5줄 + 블록 안 6줄) | 행 | 비율 |
> |---|---:|---:|
> | 캡션과 앞 숫자 일치 | 258 | 84.6% |
> | **불일치** | **9** | **3.0%** |
> | 캡션 없음 — 검증 불가 | 38 | 12.5% |
>
> 검색창을 넓혀도 회복이 멈춘다(앞 8줄+안 8줄도 동일). 즉 `claim_no` 라는 이름은 3.0%
> 에서 **거짓**이고 12.5% 에서 **미검증**이었다 — §1-3 위반이며, 그 규칙이 나온 사고
> (부채 대장 1번, `filing_date` 가 공개일이었던 건)와 같은 양식이다.
>
> **고친 방향은 이름을 사실대로 바꾸고 둘 다 담는 것이다.** 어느 쪽을 접지 키로 쓸지는
> 단계 4 가 정한다 — 여기서 하나로 뭉개면 그 판단이 사라진다. 캡션이 없으면 **비운다**
> (앞 숫자로 메우지 않는다). 리포트가 `caption_claim_resolved` 와
> `caption_differs_from_group` 을 상시 싣는다.
>
> **이 결함은 사람 대조 시트에 표 블록을 넣지 않아 드러나지 않을 뻔했다.** 초판 시트는 행
> 한 줄만 담았고, 그러면 `구성 2-3` 의 `2` 가 무엇인지 볼 수 없다. 사용자가 *"내용을 안
> 보고 어떻게 판단하나"* 라고 물어 발견됐다. **시트는 캡션 줄과 `<표 N>` 블록(최대 45줄)을
> 함께 싣는다** — 대조 불가능한 시트는 게이트가 아니라 서명란이다.

**담지 않는 것 — 명시적으로 뺀다.**

- **본문 텍스트 열을 담지 않는다.** OCR 로 잘려 있고(엄격 A 305행 중 **142행이 본문 4자 미만**),
  담으면 이름이 의미와 달라진다(§1-3). 또한 원문 발췌를 산출물에 넣으면 **공개 파생 경로의
  스크럽 대상**이 되어 §1-5 규율이 산출물마다 따라붙는다 — 담지 않으면 그 문제가 없다.
- **C 경로(행번호+판정, 611행)를 이번 산출에 넣지 않는다.** 청구항 키가 없어 접지할 자리가
  없다. 계수만 리포트에 남긴다.

### 11.2 추출 방식 — 규칙이며 LLM 이 아니다 (§5-3 결정)

| 대안 | 판정 | 이유 |
|---|---|---|
| **규칙(정규식) · 채택** | ✅ | 결정적(같은 원천 → 같은 바이트) · 스크럽 불필요 · 구조 키는 숫자와 하이픈뿐이라 OCR 에 강함 |
| 열 재구성 휴리스틱 | ❌ 이번엔 아님 | 텍스트 열을 되살리는 작업인데, 되살린 텍스트의 정확도를 검증할 준거가 없다 |
| LLM 추출 | ❌ | §1-5 — 통지서에 **심사관 실명**이 있어 `scrub_rejection_excerpts.py` 선행 필수. 그 비용을 305행을 위해 치르지 않는다. 필요해지면 별건으로 승인받는다 |

**규칙은 느슨하게 쓰지 않는다.** `<표 N>` 블록 안에서 `구성 N-M` 을 아무 데서나 찾으면
672행이지만 **45.4%(305행)만 표 행**이고 나머지는 본문 산문이다(관찰 §3.1). 그러므로
**행이 판정으로 끝나는 형태만** 받는다:

```
^\s*구성\s*(\d+)\s*[-–]\s*(\d+)\s+(.*?)\s*(실질적\s*동일|주지관용|설계변경|동일|차이|유사|대응)\s*$
```

`(.*?)` 로 잡히는 본문은 **버린다** — 잡는 이유는 앵커일 뿐이다.

### 11.3 §5 가 유보한 넷에 대한 결정

| # | 유보 사항 | **결정** | 근거 |
|---|---|---|---|
| 1 | A 의 접지 단위 | **해당 없음** — 2-A 에서 이미 결정됐고 2-C 는 다른 축(청구항 **구성요소**)이다 | — |
| 2 | B 의 목표 수 재계수 | **해당 없음** — 2-B 에서 종결 | — |
| 3 | **C 의 추출 방식** | **규칙. LLM 아님** | §11.2 |
| 4 | **결정서 `cited_evidence_map` 병합 규칙** | **병합하지 않는다** | 결정서 map 384문서·900항목 중 통지서와의 교집합이 357출원 — 인용 해소는 **2-A·2-B 가 이미 끝냈고** 2-C 는 그 결과를 재사용만 한다. 새 해소 로직을 만들지 않는다(§1-3) |
| 4′ | `citationStatus` 파생 | **범위 밖 → 단계 4로 미룬다** | Provisional/Maintained/Withdrawn 은 *통지서→결정서 상태 전이* 축이고, 2-C 산출은 *청구항 구성요소×판정* 축이다. 축이 다른 것을 한 작업에 묶지 않는다 |
| 5 | **누출 규율** | **`harvest_scope_dev_train.json` 로 자르고 회귀 테스트로 고정** | §11.4 |

### 11.4 누출 통제 — 그리고 `check_leakage.py` 가 없다는 사실

범위 원천은 `data/sources/harvest_scope_dev_train.json`(dev 200 + train 600 = 800출원 ·
`excluded_splits = [test, test_b]` · 자기 출처와 sha256 보유)이며 **새로 만들지 않는다**.

**엄격 A 305행은 전량 범위 안이다 — 누출 통제로 한 행도 잃지 않는다**(관찰 §4).

> **밝혀 둘 것.** `check_leakage.py` 는 **존재하지 않는다.** PLAN-001 §O-4 가 이름을 적어
> 두었을 뿐이고 `grep -rn check_leakage scripts/ tests/` 는 빈 출력이다. 그러므로 2-C 는
> *"check_leakage 가 0건을 검증한다"* 고 **주장하지 않는다.** 대신 산출 행의 출원이 전부
> scope 안임을 **회귀 테스트로 고정**한다. `check_leakage.py` 신설은 별건이며 여기서 하지 않는다(§1-8).

### 11.5 성공 기준 — 결과를 보기 전에 동결한다

**행 수를 게이트로 쓰지 않는다.** 305·447 은 **이미 본 수**이므로 지금 문턱이라 부르면
결과를 보고 문턱을 고르는 것이다(§1-2 · 2-A §10.10 에서 같은 실수를 한 번 했다).

| | 판정량 | 지위 |
|---|---|---|
| **주** | **사람 표본 30행 원문 대조 정확도 ≥ 0.90** — (청구항, 구성, 판정) 삼중이 통지서 원문과 일치하는가 | **아직 한 번도 측정하지 않은 지표**이므로 지금 동결하는 것이 사후 조정이 아니다 |
| 보조(게이트) | **느슨/엄격 규칙이 갈리는가** — 산문 줄이 산출에 0건 | 표본 30행에 산문이 하나라도 있으면 실패 |
| 서술통계 | 행 수 · 출원 수 · 판정 라벨 분포 · scope 손실률 | 게이트 아님. 그대로 보고 |

- **실패해야 할 입력이 실패하는가**: 본문 산문 줄·판정 없는 줄·`구성 N-M` 이 없는 표 행을
  합성 델타로 넣어 **0건 추출**을 단위 테스트로 고정한다.
- **재현성**: 두 번 돌려 parquet 바이트 동일.

> **§11.5-a 계측기 교정 이력 (2026-09-06) — 지우지 않는다.**
>
> 게이트를 두 번 받았고, 그 사이에 **계측기를 두 번 고쳤다.** 2-A 가 "여섯 번의 계측기
> 교정" 을 남긴 것과 같은 이유로 여기 남긴다 — 교정 전 수를 지우면 최종 수가 무엇을
> 거쳐 나왔는지 알 수 없다.
>
> | 교정 | 무엇이 틀렸나 | 발견 경로 |
> |---|---|---|
> | 1 | `구성 N-M` 앞 숫자를 `claim_no` 라 불렀다 (§11.1-a) | **사용자 지적** — *"source_file 만 주어져 있는데 내용을 안 보고 어떻게 판단하나"* |
> | 2 | 대조 시트가 **다른 표의 캡션**을 보여줬다. 빈 줄 없는 문서에서 앞 표의 창이 뒤 표의 행을 삼켰다 | **1차 대조 25/30 (0.8333)** — 오답 5건이 **전량** 이 형태였다 |
>
> **1차 대조의 `0` 다섯 건은 검토자가 옳았다.** 보이는 캡션이 `caption_claim_no` 와
> 모순됐으니 `0` 이 맞는 반응이고, 다섯 행 모두 원문 대조에서 parquet 값이 맞았다.
> **판정이 옳았고 계측기가 틀렸다.**
>
> | | 값 |
> |---|---:|
> | 1차 (교정 전) | 25/30 = **0.8333** — 미달 |
> | 2차 (교정 후, 같은 표본·시드) | 30/30 = **1.0000** — 충족 |
>
> **파서는 두 교정 모두에서 무사했다** — 블록 닫힘 규칙을 바꿔 전수 재계산해도 305행
> 그대로이고 결과가 다른 문서는 0건이다. 결함은 이름과 시트에 있었다.
>
> **사람이 채운 시트는 별도 파일(`*_sample_result.csv`)로 받는다.** `--sample` 은 시트를
> 덮어쓰므로 같은 파일에 채우게 하면 재생성 한 번으로 사람의 작업이 지워진다 — 교정 중
> 실제로 두 번 재생성했다. 게이트는 결과 시트가 **현재 표본과 같은 행인지 확인**하고,
> 다르면 통과시키지 않는다.

### 11.6 하류 파급 (§0)

**없다.** 산출은 `data/patents/` 의 parquet 하나이고 **TTL·TBox·shape·IRI·어휘를 건드리지
않는다.** 그래프 서명은 불변이며 스냅샷을 핀한 소비자가 갱신할 숫자는 없다. 이 데이터가
그래프에 들어가는 것은 **단계 4**이며 그때 별도 승인을 받는다.

### 11.7 비목표

1. 텍스트 열 복원 · 열 재구성 휴리스틱
2. LLM 추출과 그에 필요한 스크럽 파이프라인
3. C 경로(청구항 키 없는 611행)의 산출물 편입
4. `coveredBy`·`uncoveredConcept` **술어 신설** — 단계 4
5. `citationStatus` 파생 — 단계 4
6. `check_leakage.py` 신설 — 별건
7. 결정서 `cited_evidence_map` 과의 병합

---

## 12. 단계 4 설계와 실행 기록 — T-Box·R-Box 신설 (2026-09-06 · §2 3단계 별도 승인 🛑 완료)

**결론 먼저.** `pa:` core(224 트리플 · 명명 클래스 13 · OP 23 · DP 9) + 도메인 바인딩(74) +
KR 관할 바인딩(41) 을 `scripts/build_priorart_modules.py` 로 지었고, §5 V6(a) CI 불변식 둘을
**같은 커밋에서** 세웠다. 기존 T-Box 는 한 줄도 바뀌지 않았다.

### 12.1 2-C 이월 결정 — 접지 키는 `caption_claim_no` 다 (실측)

`notice_element_judgments.parquet` 305행 × `mappings/claim_features.parquet` 조인 실측:

| 측정 | 값 |
|---|---:|
| `caption_claim_no` 로 실재 청구항 접지 | **235 / 305 (77.0%)** |
| `element_group` 의 겉보기 접지 | 273 / 305 — 그러나 **캡션 불일치 9건 전량이 다른 실재 청구항에 붙는다** |
| 캡션 결측 38행에 `element_group` 사용 시 | 38행 전부 접지되나 **검증 준거 없음** |
| `element_group` 이 표를 넘나드는가 | **0건**(65 그룹) — 표 지역 번호임이 확정 |
| 심사관 `element_no` 최대 = 우리 `feature_seq` 최대 | **8 / 53 (15.1%)** |

**결정 ①** `pa:concernsClaim` 은 캡션 청구항만 받는다. `pa:elementGroup` 은 **데이터 술어**라
구조적으로 청구항을 가리킬 수 없고, SHACL 이 `concernsClaim` 을 필수로 건다.
**결정 ②** 요소↔요소 정렬 술어를 만들지 않는다 — 15.1% 일치는 다른 분해라는 뜻이다.

### 12.2 계획 문면과 달라진 다섯 곳 (전부 승인 시점에 명시)

| # | 계획 문면 | 실행 | 이유 |
|---|---|---|---|
| 1 | 도메인 바인딩 이름공간 `semi:` | 기존 **`ont:`** 사용 | `build_owl.py` 의 `SEMI` 가 이미 SemicONTO — 접두어 하나가 두 뜻이면 §1-3 위반 |
| 2 | §8 이 `sdkb-claim-semantics.ttl` 과 `pa:` 3모듈을 **둘 다** 열거 | `pa:` 3모듈만 신설 | §1.10(c) 가 §1.2(b) 를 대체한 뒤로는 같은 어휘의 두 집이 된다 |
| 3 | 신규성 태스크 질의를 CQ 스위트에 편입 | 편입하되 **`expect-min: 0`** | A-Box 가 단계 5 산출이라 지금 0행이 정상. 1 로 올리는 것은 **단계 5 와 같은 커밋** — 문턱을 만져 통과시키지 않는다(§1-6) |
| 4 | 술어 목록에 없던 `pa:discloses` 추가 | 추가 | `Disclosure` 가 도달 목표 노드로 선언만 되고 질의가 닿을 수 없었다 — 신규성 우변이 없으면 CQ32 가 성립하지 않는다 |
| 5 | `pa:underJurisdiction` 의 `rdfs:domain` | **선언하지 않음** | `MinedAxiom` 에 걸면 `LegalGround` 개체가 추론으로 `MinedAxiom` 이 된다. 필수 조건은 SHACL 이 진다 |

### 12.3 게이트 실행 결과 (재현 명령 병기)

```
make priorart      → core 224 · semi 74 · kr 41 트리플 · 두 번 빌드 sha256 동일
make validate      → exit 0. 불변식 A OK(도메인·관할 IRI 0) · 불변식 B OK(1건 검사)
                     shapes_priorart × 3모듈: LegalGround 2 · DocumentType 2 검사, 
                     MinedAxiom·ExaminerElement 타깃 0 (vacuous 로 출력 — 숨기지 않는다)
make test          → 352 passed · 10 skipped (신규 tests/test_priorart_modules.py 22건 포함)
```

**게이트가 무는 것을 확인했다.** core 에 `ont:`·`gov:`·`pa/kr/`·SemicONTO IRI 를 각각
주입하면 전부 실패하고, 태스크 질의 필수부의 행정 어휘·UNION 가지·파싱 불가 질의도 실패하며,
`OPTIONAL` 안의 같은 어휘는 통과한다. **이 회귀가 실제 결함 하나를 잡았다** — 허용 목록이
`pa/` 접두어 비교라 `pa/kr/…` 가 core 를 통과했다(고침: 로컬명에 `/` 가 없을 때만 자기 자신).

### 12.4 하류 조치 (§0 — 자동으로 도달하지 않는다)

하류 `sdkb-prior-art-paper` 의 vendor 대상은 하드코딩 목록이라 신규 3종이 복사되지 않는다.
`ontology/vendor.py:VENDOR_FILES` **와** `ontology/baseline.py:BASELINE_PARTS` **양쪽에**
넣어야 한다 — 하나만 고치면 스냅샷에는 들어오고 G₀ 에는 안 들어온다(하류 PLAN-045 D5).
기존 `sdkb-patent.ttl` 의 핀 sha256 `0a317389…9829` 은 **불변**임을 확인했다.

### 12.5 다음 (단계 5 이후에 넘기는 것)

`propertyChainAxiom` 넷(§1.9(b) 후보생성기) · `combinableWith`(진보성 관통) ·
~~`ClaimProfile`/`Disclosure` A-Box 실체화 · CQ32 `expect-min` 1 승격~~(→ §13 · 5-A 완료) ·
CQ10 의 행정 어휘 필수부 사용(단계 6·7) · `citationStatus` 파생.

---

## 13. 단계 5-A 설계와 실행 기록 — A-Box 실체화 (2026-09-07 · §2 3단계 🛑 사용자 승인)

**결론 먼저.** `scripts/build_abox_priorart.py` 가 `pa:ClaimProfile` **55,510** ·
`pa:Disclosure` **31,114** · `pa:ExaminerElement` **235** · `pa:broaderConcept`=`coveredBy` **16**
을 `ontology/sdkb-abox-priorart.ttl`(675,934 트리플 · gitignore)에 실체화했고, 단계 4 가
vacuous 로 남긴 셋 — CQ32(0 → 200행 · `expect-min` 1) · SHACL priorart shape(실 타깃에 위반 0) ·
V2 목표 노드 `Disclosure` — 이 처음 성립했다. 미매핑률 리포트
`data/reports/abox_priorart_report.json` 이 **5-B 의 기준선**이다.

### 13.1 사용자 결정 셋 (착수 전 · AskUserQuestion)

| # | 결정 | 이유 |
|---|---|---|
| 1 | **단계 5 를 5-A(실체화) / 5-B(접지율 개선)로 나눈다.** 이번은 5-A | 접지율 개선(구조요소 15개 등록 → `concept_mapping` 재생성 → 130만 feature 재접지)은 906 MB TTL 과 `claim_features.parquet` 의 sha 를 바꾼다 — 파급이 크고, 5-A 의 미매핑률 리포트 없이는 개선을 잴 눈금이 없다 |
| 2 | **core 에 OP 둘 추가** — `pa:profileOf` · `pa:disclosureOf` | 단계 4 T-Box 에 ClaimProfile↔청구항 · Disclosure↔문헌 연결 술어가 없었다(실측). 대안(ClaimVersion 경유 · `prov:wasDerivedFrom`)은 14만 개의 자명한 노드를 만들거나 이름이 의미를 약하게 담는다(§1-3) |
| 3 | **미바인딩 개념 34종은 제외하고 계수** | `FailureMode` 16 · `Skill` 11 · `EquipmentClass` 7 은 semi 가 `pa:TechnicalConcept` 에 걸지 않았다. 넣으면 `sh:class` 가 문다. 단계 4 의 바인딩 결정을 이번에 건드리지 않고 `EquipmentClass` 바인딩은 5-B 의 측정 가능한 레버로 둔다 |

### 13.2 설계 요지

- **Disclosure 는 문헌 단위**다 — 신규성이 *"요소 집합 ⊆ 단일 문헌 개시 집합"* 이므로.
  `discloses` = 문헌 전 청구항 개념의 합집합. **ClaimProfile 은 독립항 단위**, `essentialConcept`
  = 그 항의 개념, `optionalConcept` = 그 항을 루트로 갖는 종속항 개념(essential 제외 · 루트는
  `depends_on_claim` 메모이즈 DFS · 다중 루트면 전부). 개념 0 인 독립항은 만들지 않는다 —
  좌변이 공집합인 프로파일은 어떤 문헌에도 "덮인" 것이 된다. **cited 측도 같은 경로**로
  방출한다(PLAN-001 §1.4-2 동일 해상도).
- **바인딩 타입은 T-Box 에서 읽는다.** semi 의 `subClassOf* pa:TechnicalConcept` → core-data 의
  개념 개체 → CURIE 집합. 하드코딩 0.
- **`coveredBy` 1홉 실체화, 전이 폐쇄 없음.** `run_cq.py` 는 추론기를 돌리지 않으므로
  `broaderConcept ⊑ coveredBy` 함의를 단언으로 두지 않으면 확장이 0건 작동한다. §3.3 의
  확장 깊이 {0,1} 은 질의의 `pa:coveredBy?` 가 든다. 단계 6 V1 절제는 둘을 함께 뺀다.
- **ExaminerElement 는 캡션 청구항이 그래프에 실재할 때만.** IRI
  `examiner_element/{stem}_t{표}_g{그룹}_e{번호}` · `assertedIn examdoc/{stem}`(stem =
  통지서 파일명의 출원번호+발송번호 — 행정 식별자이며 성명이 아니다 · `check-public` 0 적중).
  `dcterms:source` 는 생성기→parquet 이고 파일명을 담지 않는다(2-B 선례). scope 밖 행이
  있으면 `SystemExit`.
- **원천에 없는 것은 내지 않는다** — `issuedDate`·`examRound` 는 2-C parquet 에 없다(§1-4).

### 13.3 동결 기준 → 실측 (재현 명령 병기)

| 기준 | 동결(실행 전) | 실측 | 명령 |
|---|---:|---:|---|
| ClaimProfile / Disclosure | 55,510 / 31,114 | **55,510 / 31,114** | `make abox-priorart` |
| ExaminerElement / ExaminationDocument | 235 (=305−38−32) / 19 | **235 / 19** | 〃 · 리포트 `examiner_elements` |
| broaderConcept = coveredBy | 16 = 16 · 폐쇄 0 | **16 = 16** | 리포트 `hierarchy` |
| 결정성 | 두 빌드 sha256 동일 | **동일** (`41ca3ac46dff…`) | 두 번 빌드 후 `sha256sum` |
| SHACL | 위반 0 · 세 타깃 non-vacuous | **PASSED** · 55,510 / 31,114 / 235 검사 · MinedAxiom 0(vacuous) | `make validate` |
| CQ32 | ≥1행 · 불변식 B 통과 | **200행 · 0.05초** · B OK | `make cq` · `check_priorart_invariants.py` |
| 술어 계약 · 인라인 선언 | ⊂ T-Box · 0 | 통과 | `tests/test_abox_priorart.py` |
| 누출 | ExaminerElement 출원 ⊂ scope 800 · 인용 간선 0 | 통과 (out_of_scope 0) | 〃 |
| 공개 경계 | check-public 0 · TTL gitignore | **적중 0 · 363 파일** · 미추적 아님 | `make check-public` · `git status` |
| 테스트 | 기존 전량 + 신규 | **374 passed · 10 skipped** | `make test` |

**미매핑률 (5-B 기준선 · 바인딩 개념 88종 기준).** 독립항: rej 3,541 중 미매핑 998
(**28.2%**) · cited 60.5% · g1 62.1% · g2 62.7%. 문헌: rej 9.3% · cited 9.8% · g1 28.2% ·
g2 15.5%. 제외된 34 개념 때문에 빠진 프로파일 2,018 · Disclosure 694. 종속항 개념 중
루트 독립항에 프로파일이 없어 버린 것 23,208 링크(`optional_dropped_root_has_no_profile`).

### 13.4 계측기 교정 (전후를 남긴다)

① 단계 4 테스트를 `expect-min: 1` 로 갱신하며 "ORDER BY 없음" 검사를 파일 전체에 걸었고,
CQ32 주석("ORDER BY 를 두지 않는다")에 걸려 실패했다 → 본문(주석 제외)만 보도록 고쳤다.
② 커밋돼 있던 `cq_report.json`(2026-08-09)과 새 실행의 기존 CQ 행 수가 달라 보였다.
priorart 넷을 뺀 옛 그래프 목록으로 다시 돌려 대조 → **31개 전부 동일, CQ32 만 0 → 200**.
차이는 8/25·9/6 의 A-Box 재빌드에서 온 것이다. ③ 병렬로 `signature-inject`·`check-public`
이 파일을 다시 쓰는 동안 돌린 첫 `make test` 에서 1건이 실패했으나 출력이 잘려 이름이 남지
않았다. 아무것도 돌지 않는 상태에서 재실행 → 374 passed.

### 13.5 하류 조치 (§0)

`sdkb-priorart-{core,semi,kr}.ttl` 의 sha256 이 바뀌었다(core·semi 어휘 추가 · kr 은
`dcterms:modified`). 하류 `vendor.py:VENDOR_FILES` 와 `baseline.py:BASELINE_PARTS` 를
**양쪽** 갱신할 것. A-Box 는 gitignore 라 git 으로 오지 않으며 `make abox-priorart` 로
로컬 빌드한다(결정적 · 자격 불필요). 단계 7 재측정 시 `report_priorart_baseline.py` 의
V2 목표 노드를 인용문헌 노드(퇴화형)에서 `disclosure/{publication_id}` 로 교체한다.

### 13.6 다음

**5-B · 접지율 개선(§2 1단계 🛑 별도)** — 기준선은 §13.3 의 미매핑률. 레버 후보:
구조요소 15개(`ko_concept_proposals.json`) → `ont:StructuralElement` 등록 · `EquipmentClass`
바인딩 · concept_mapping 재생성 → 재접지. 그 뒤 단계 6(V1 절제) · 7(V2–V4 재측정 ·
τ 동결) · MinedAxiom 실체화(PLAN-002 채굴 쌍이 이 저장소에 들어온 뒤). → **§14 로 실행됐다.**

---

## 14. 단계 5-B 설계와 실행 기록 — 개념 접지율 개선 (2026-09-07 1단계 · 2026-09-08 2–5단계 · 사용자 승인)

**결론 먼저.** feature 접지 **33.1% → 38.6%** · rej 독립항 미매핑 **28.2% → 24.0%**. 동결 목표 전부 충족.
변동은 L0(링커 모드) · L1(`EquipmentClass` 바인딩) · L2(구조요소 15 등록) 로 분리 측정했고 설명되지 않는
변동은 0 이다. 요약과 하류 조치는 CHANGELOG 2026-09-08 항목, 계획 전문은 세션 계획 파일에 있다.

### 14.1 1단계 — 동결 목표 (2026-09-07 · 결과를 보기 전에)

| 눈금 | 기준선(5-A) | 동결 목표 | 실측(L2) |
|---|---:|---:|---:|
| rej 독립항 미매핑 | 28.18% | **≤ 25.0%** | **23.95%** ✓ |
| feature 접지 | 33.10% | **≥ 37.0%** | **38.58%** ✓ |
| g1 미매핑 | 62.12% | ≤ 59.12% (−3pt) | **56.07%** ✓ |
| g2 미매핑 | 62.65% | ≤ 59.65% (−3pt) | **58.66%** ✓ |
| 행 수 | 1,306,191 | 불변 | 1,306,191 ✓ |

사용자 결정 셋(1단계): ① 목표를 위 표로 동결 ② 구조요소 **15개 등록 + 7개 R7 차단 유지**(규칙 불변 · 효과는
8개분) ③ R7 예외는 별도 안건. 앞 세션이 "확보했다"고 한 목표 수치는 저장소에 기록돼 있지 않았고, 이 표의
목표는 parquet 실측 상한(L1 26.3% · L2-8 +76건 · 합 ≈24.1%)에서 다시 계산해 동결한 것이다.

### 14.2 2단계 — 실측 넷

| 확인 항목 | 사실 |
|---|---|
| 개념 등록 경로 | `convert_rdf.py:88` 은 타입을 검사하지 않고 IRI 를 찍는다 · `build_owl.py` 는 고정 dict 라 클래스 선언이 따로 필요 · 사전은 canonical·id-local·synonym 을 자동 수집 · **링커 타입 필터 `CONCEPT_TYPES` 에 StructuralElement 가 없어 이것 없이는 재빌드가 0 효과** · `build_abox_priorart.py` 는 semi 의 `subClassOf*` 를 런타임에 읽어 변경 0 |
| 재접지 비용 | `make abox-claim-features` 실측 **34분 · RSS 17.7 GB**(문서의 25분은 substring 모드) · 입력 전부 로컬 · 네트워크·LLM 0 |
| parquet sha 핀 | 저장소 안 핀은 `abox_priorart_report.json.inputs`(리포트 · 자동) 뿐 · 테스트는 산출 TTL sha 만 단정 · 공개 리포는 parquet 제외·meta 포함 · 하류 `VENDOR_FILES` 가 벤더 |
| 레버 분리 순서 | L0 → L1(semi + abox-priorart 30초) → L2 · 각 단계 리포트 스냅샷 · `concept_df` 차분으로 개념 단위 귀속 |

**2단계에서 걸린 것 — 링커 모드.** `.venv` 에 kiwipiepy 가 없었고 생성기는 조용히 substring 으로 떨어졌다.
사용자 결정(2026-09-08): Kiwi 설치 + KG 변경 전 재현 사전검사 → **sha 불일치**(`16f8300d…` → `bce4b9f0…`) →
원인 실측(커밋 parquet 는 substring 산출물) → **Kiwi 모드를 새 기준선(L0)으로**. 생성기는 폴백을 버리고
모드·버전을 `release_meta.source.linker` 에 기록한다.

### 14.3 3단계 — 설계 요지

- **T-Box/shape**: semi 바인딩 +`EquipmentClass`(배제 없음 · `MODIFIED` 불변) · core `StructuralElement` 클래스
  선언(`pa:` 언급 없이) · `featureConcept` range 합집합 +`StructuralElement` · `Shape_CoreNode`·`Shape_Provenance`
  타깃 +`StructuralElement`.
- **원천**: `scripts/add_structural_elements.py`(상위개념 인젝터 선례 · 멱등 · `lexicon_profile: patent-text` ·
  synonyms 에 한글 1건씩 · provenance author/CDLA). id 접두 `structural_element:` · 범용 영단어는 canonical 을
  `… Region` 으로.
- **생성기**: `CONCEPT_TYPES` +`StructuralElement` · 링커 모드 기록 · 폴백 제거. `AXIS_RANK`·특허 단위 라우팅은 불변.
- **테스트**: 깨지는 넷(타입 집합 · 274 상수 · 사전 원장 · README 서명) 같은 커밋에서 · 신규
  `tests/test_stage5b_grounding.py` 에 생성기↔range↔semi 양방향 계약과 **동결 목표 게이트**.

### 14.4 4·5단계 — 실행과 검증 (재현 명령 병기)

| 단계 | 명령 | 결과 |
|---|---|---|
| 0 | `uv sync --extra nlp` · 재현 사전검사 | kiwipiepy 0.23.2 · **불일치 → L0 로 채택**(위) |
| L0 | `python scripts/build_abox_priorart.py`(semi 불변) | 접지 435,044 · rej 27.90% · 미바인딩 35 |
| L1 | `make priorart` → `build_abox_priorart.py` | rej 26.04% · g2 59.37% · 미바인딩 28 · semi 77 트리플 |
| L2 | `make structural-elements parse owl convert concept-mapping abox-claim-features` → `build_abox_priorart.py` | 접지 503,960(38.58%) · rej 23.95% · 개념 138 · ClaimProfile 62,926 · Disclosure 33,274 |
| 결정성 | `make abox-claim-features` 재실행 · `abox-priorart` 재실행 | **바이트 동일** — parquet `1e8b14ca…` · meta `9af97816…` · TTL `5069d7e0…`(12,001,973) · priorart `4c726e98…`(796,656) · 리포트 `cmp` 동일 · 각 34분 |
| 게이트 | `make validate` · `make cq` · `make signature-inject` · `make test` · `make public-release && make check-public` | validate **PASSED**(StructuralElement 타깃 15 · 8분 12초) · CQ32 200행 pass · 서명 1,926 → 1,932 · **382 passed · 10 skipped** · check-public 365 파일 적중 0 ✅ |

귀속(개념 단위): L1 = `equipment_class:*` 7종 11,612 + 종속항 간접 602 · L2 = 신규 12 개념 + `material:sin` +77
(Kiwi 사용자 사전 비국소 효과 · `질화규소` → `질화 규소`) · 그 밖 **0**.

### 14.5 남긴 것 (별도 안건)

R7 차단 7개(rej 상한 ≈14% · 단 `기판` 하나가 210,773 feature 에 걸린다) · Kiwi 모드의 1글자 원소기호
재유입(`W` → tungsten +2,289 · CR-013 계열) · 영문 `drain`·`channel`·`stack` 의 US 측 정밀도 관찰 ·
FailureMode·Skill 바인딩(태스크 축 · D-15). **다음**: 단계 6(V1 절제) · 7(V2–V4 재측정 · τ 동결).

---

## 15. 단계 6 설계와 실행 기록 — V1 절제 (2026-09-08 · 1단계 재제출 → 계획 승인 · 6-A 실행)

**결론 먼저 (6-A).** V1 계측기를 §5 정의대로 완성해 R-Box·바인딩 **37건**을 절제했더니 소비 **8**
(`broaderConcept ⊑ coveredBy` 1 + 인스턴스가 있는 바인딩 7) · 미소비 29 이고, **결과를 보기 전에 동결한
예측표와 37건 전부 일치**했다. 그래프 바이트는 바꾸지 않았다 — A-Box sha `4c726e98…` 불변. 삭제·CQ33·게이트는
6-B(§2 3단계 별도 승인 🛑)로 넘긴다. 대조표는 기계가 렌더한다: `01.code_spec/reports/PLAN-005-stage6-axiom-consumers.md`.

### 15.1 다시 세운 이유 — 첫 초안이 놓친 것

첫 1단계 초안("절제기 돌리고 미소비를 지운다")은 계획서·CHANGELOG·코드 전량 재조사에서 열두 가지를 놓친
것으로 드러났다(계획 파일 Context 표 · 사용자 검토 지시). 골자만 적는다.

| 누락 | 사실 |
|---|---|
| 계측기가 ②만 구현 | `report_priorart_baseline.py` 는 CQ 행 수만 본다. 추론기 없는 rdflib 에서 T-Box 공리를 빼도 BGP 행 수는 **구조적으로** 불변 — 단계 1 의 "8건 중 소비 0" 은 공리의 무의미가 아니라 계측기의 맹점이었다 |
| 신설 공리가 보이지 않음 | `ont:` 주어만 세고 TBOX 목록에 priorart 3모듈이 없어 단계 4 의 core 6 · semi 9 · kr 3 이 열거에서 빠졌다 |
| 소비가 하드코딩 | `build_abox_priorart.py:356-358` 이 broaderConcept → coveredBy 를 고정 방출해 공리를 빼도 A-Box 가 그대로였다 — 설계 중심 공리가 미소비로 판정될 자리 |
| CQ32 포화 | `LIMIT 200` 에 닿아 있어(rows=200) 행 수로는 coveredBy 소비가 보이지 않는다. LIMIT 제거는 134,762 × 33,274 크로스곱이라 불가 |
| 이월 항목 | §12.5 가 단계 6 로 넘긴 propertyChainAxiom 넷 · CQ10 행정 어휘 교정 · citationStatus 파생을 초안이 전부 비목표로만 적었다 |
| 순환 | chain 넷은 자원에 없고(§7-6 으로 보류) §9-5 는 "자원에 남은 공리"만 본다. PLAN-004 C-2 는 반대로 "각 0 을 벗어나고"를 요구 → 문면 충돌 |
| 구 8건의 자리 | 6건은 손으로 쓴 `sdkb-patent.ttl`(하류 핀 `0a317389…`) — 삭제 불가 |
| C5 오염 경로 | `skos:exactMatch ⊑ coveredBy` 는 kr 의 `Ground_29_x skos:exactMatch Rejection_*` 와 core 의 클래스 정렬 23건까지 coveredBy 로 끌어들인다 |
| C1 모순 | `broaderConcept` 전이 선언은 동결 깊이 {0,1}(§3.3) 과 모순 — 추론기가 붙는 순간 coveredBy 가 사실상 전이 |
| 단계 3 미수행 | PLAN-002 `Prec ≥ 0.60` 은 산출된 적이 없다(2인 코더 배정 대기). 단계 4 는 **어휘만** 지었으므로 "공리 신설" 선행조건에 걸리지 않았다는 해석을 여기 처음 적는다. MinedAxiom·substitutableWith 유량 0 의 구조적 이유 |
| τ | 단계 7 의 절대 하한 τ 는 어디에도 동결돼 있지 않고 V2 기준선은 이미 본 수다 → **단계 7 1단계에서 결과를 보기 전에 정의**한다(§11.5·§10.10 의 실패 형태 재발 방지) |

### 15.2 사용자 결정 (2026-09-08)

| # | 결정 |
|---|---|
| D1 | 단계 6 을 **6-A(계측 완성·절제 측정) / 6-B(삭제·CQ·게이트)** 로 나눈다. 6-A 에 생성기 변경 1건(coveredBy 실체화 대상을 core 의 `⊑ pa:coveredBy` 공리에서 읽음)을 포함하되 **A-Box sha `4c726e98…` 바이트 불변**이 승인 조건 |
| D2 | `substitutableWith`(대칭 · ⊑coveredBy): **보존 + 일몰** — 단계 7 착수 시점까지 PLAN-002 3단계(Prec) 미착수면 삭제. `skos:exactMatch ⊑ coveredBy` 는 삭제 |
| D3 | semi `disjointWith` 4건: **불변식 C 신설**(배제쌍 동시 타이핑 개체 0)로 소비자를 부여하고 validate 에 배선 |
| D4 | CQ10: **CQ33 신설**(pa:Disclosure 기반 · task-neutral) + CQ10 은 헤더에 "증거층 CQ" 명기·불변 |

기본값(계획 승인): 구 8건 보존(§7-2 · 핀) · **chain 넷 미투입**(무추론 §3.3 배치에서 소비자가 성립할 수 없고
실체화하면 §1.9(b) 의 동점 블록 · 후보생성은 V2 Reach 가 parquet 로 수행 → PLAN-004 C-2 문면 개정안을 6-B 에서
제안) · inverseOf 3·K3·C1 은 예측대로면 6-B 삭제 · S18–S20·K1–K2 는 바인딩으로 재분류 · citationStatus·combinableWith
·MinedAxiom 실체화는 별건.

### 15.3 동결한 조작적 정의와 예측 (결과를 보기 전에)

공리 a 가 **소비된다** ⟺ O∖a 에서 ② CQ 32개 행 수 벡터 · ③ 생성기 실체화 입력(coveredBy 쌍 수 · 바인딩 개념 수) ·
④ 태스크 커버 — "u 가 프로파일 p 의 필수개념이고 u 의 coveredBy 상위 f 를 문헌 d 가 개시하되 u 자체는 개시하지
않는" (p,u) 쌍 수 — 중 하나가 변한다. **경로**(읽는 소비자가 있다)와 **유량**(이 데이터에서 결과가 변했다)을
분리한다. ④ 는 파이썬 집합 계수다 — 같은 정의의 rdflib COUNT 는 15분 넘게 미완(실측 · 중단).
예측표는 `scripts/report_v1_ablation.py:FROZEN`(37건)이며 테스트가 열거와 표의 일치를 고정한다 — 결과를 본 뒤
표를 늘리지 못한다.

### 15.4 6-A 실행 (재현 명령 병기)

| 항목 | 명령 | 결과 |
|---|---|---|
| A1 생성기 T-Box 주도화 | `make abox-priorart` | 796,656 트리플 · sha **`4c726e98…` 불변** · 리포트에 `covered_by_by_subproperty {broaderConcept 16 · substitutableWith 0 · exactMatch 0}` 추가 · 원천 표에 없는 `⊑ coveredBy` 술어는 `SystemExit` |
| 스팟체크(원천 사실) | 읽기 전용 | `skos:broader` 18쌍 중 길이-2 사슬 **0** · core-data `exactMatch` **0**(sdkb-core 클래스 정렬 23) · plasma_etch 개시 Disclosure **558** · ④ 기준선 (p,u) **4,306** · (p,d,u) 40,024,841 · 1.5초 |
| A2 절제 ③④ | `report_v1_ablation.py --skip-cq` | 37건 · 소비 8 · 미소비 29 · **예측 불일치 0** · 64초 |
| A2 절제 ② 전량 | `make v1-ablation` | 26건(rbox 21 + 바인딩-술어 3 + 바인딩-매치 2) 전부 **변화 없음** — 예측대로 구조적 0 · 기준선 32 CQ · 9,670행 · 회당 145–161초 · 합 3,913초(65분) · subClassOf 바인딩 11건은 ② 를 건너뛰고 ③ 이 검출 |
| 게이트 | `make validate` · `make test` | `make validate` **exit 0**(불변식 A·B OK · SHACL 6블록 PASSED) · `make test` **392 passed · 12 skipped**(6-A 신규 12건 포함) · `make v1-ablation` exit 0 · 예측 불일치 **0** · `make check-public` **367 파일 · 적중 0 ✅** · `make signature-check` 최신(T-Box 불변) |

소비 8 의 내역: `broaderConcept ⊑ coveredBy`(coveredBy −16 · ④ −4,306) · 바인딩 Material(coveredBy −15 · bound −31 ·
④ −3,945) · Process(−1 · −12 · −361) · SubProcess(−1 · −38 · −361) · Device(bound −34) · EquipmentClass(−12) ·
Parameter(−5) · StructuralElement(−15). 경로만 있고 유량 0: `substitutableWith` 대칭·⊑coveredBy · `skos:exactMatch ⊑
coveredBy` · Problem·ProcessCondition·TechnicalEffect·TechnicalFunction 바인딩(core-data 인스턴스 0).
pa: 모듈의 R-Box 미소비 12건 = C1 · C3 · C4 · C5 · C6 · S16 · S17 · S12–S15 · K3 — 6-B 의 삭제·유예·불변식 C 후보이며
**6-A 는 아무것도 지우지 않았다.**

### 15.5 하류 조치 (§0)

6-A 는 TTL 바이트를 바꾸지 않는다 — vendor 목록·sha 핀 변동 0. 리포트 `abox_priorart_report.json` 은 키 추가만
(`inputs` 에 core sha · `hierarchy.covered_by_*`). 6-B 가 core/semi/kr sha 를 바꾸며 그때 `VENDOR_FILES`·`BASELINE_PARTS`
양쪽 통보.

### 15.6 다음

**6-B**(§2 3단계 별도 승인 🛑): C1·C5·C6·S16·S17·K3 삭제(역술어는 선언까지) · C3·C4 보존+일몰 주석 · 불변식 C ·
CQ33 신설 + CQ10 증거층 명기 · `test_stage6_ablation.py` 2부(RETAINED 허용목록 게이트) · `make signature-inject` ·
PLAN-004 C-2 개정안 · 절제 재실행으로 "pa: R-Box ∖ RETAINED 중 미소비 0" 확인. 그 뒤 단계 7(τ 를 결과 전에 동결).

### 15.7 6-B 실행 기록 — 삭제 · CQ33 · 불변식 C (2026-09-09 · §2 3단계 별도 승인 · 사용자 승인)

**결론 먼저.** 미소비 12건 중 **6건 삭제**(`broaderConcept` 전이 · `skos:exactMatch ⊑ coveredBy` · 역술어 셋 선언까지 ·
`differentFrom`), **6건 보존에 사유**(`substitutableWith` 2 = 일몰 · `disjointWith` 4 = 신설 불변식 C 가 읽음). 절제
재실행으로 §9-5 를 **`pa:` 3모듈 범위**에서 닫았다 — "pa: R-Box ∖ RETAINED 중 미소비 0". 구 8건은 §7-2·하류 핀으로
보존하며 §9-5 의 "자원" 은 이 범위로 읽는다(15.2 기본값). A-Box sha `4c726e98…` 불변.

| 항목 | 명령 | 결과 |
|---|---|---|
| B1 삭제 | `make priorart` · `--check` | core 232 → **226** · semi 77 → **65** · kr 41 → **40** · OK |
| A-Box | `make abox-priorart` | sha **불변** · `covered_by_by_subproperty {broaderConcept 16 · substitutableWith 0}` |
| B2 불변식 C | `check_priorart_invariants.py --data core-data abox-priorart` | 4쌍 · 개체 **96** 검사 · 위반 0 · 주입 위반(직접·SubProcess 경유)은 문다 |
| B3 CQ33 | `make cq` | **540행**(plasma_etch 개시 558 중 2015 이후 출원 제외) · 33 CQ · 29 통과 · 불변식 B 대상 2 |
| 서명 | `make signature-inject` | Total 1,932 → **1,913** · OP 126 → **123** |
| 절제 재실행 | `make v1-ablation` | 31건(삭제 6 반영) · 소비 8 · 미소비 23 · **pa: R-Box 미소비 6 = RETAINED 정확히**(substitutableWith 2 · disjointWith 4 · 전부 경로 有) · 예측 불일치 0 · ② 20건 전부 무변화(회당 151–157초 · 합 3,049초) · CQ 33개 10,210행 · ④ 기준선 4,306 불변 |
| 게이트 | `make validate` · `make test` · `make check-public` | `make validate` **exit 0**(불변식 A·B·C OK · SHACL 6블록 PASSED) · `make test` **400 passed · 10 skipped**(6-B 신규 8건 포함 · RETAINED 게이트 통과) · `make cq` 33개 29 통과 · `make check-public` **369 파일 · 적중 0 ✅** · `make signature-inject` 반영 |

sha256(앞 12): core `67e6693f8aac` → `e565b3257188` · semi `667a6f4bc164` → `d66c93252478` · kr `f19a7e646081` → `4e5ed9739f64`.

**PLAN-004 C-2 개정안 (제안 · PLAN-004 편집은 별도 승인).** 현 문면(PLAN-004:108) *"`inverseOf`·`propertyChainAxiom`·
`disjointWith` 가 각 0 을 벗어나고, 각 공리가 어느 CQ 또는 태스크 경로에 쓰이는지 기계로 확인됨"* 은 앞 절과 뒤 절이
충돌한다 — 6-A 절제가 `inverseOf` 3건 전부를 소비자 없음으로 판정했고, `propertyChainAxiom` 은 추론기 없는 §3.3
배치에서 소비자가 성립할 수 없다. 개정안: *"R-Box 공리마다 **소비 경로가 기계로 확인**된다(PLAN-005 §5 V1 절제 ·
경로/유량 구분). 종류별 최소 개수는 두지 않는다 — 개수 조건은 표를 채우려고 공리를 만들게 한다(§7-6)."* 종류별 0 이
결손인 것이 아니라, 소비자 없는 공리가 결손이다.

**단계 3 미수행의 지위.** PLAN-002 `Prec ≥ 0.60` 은 2인 코더 배정 대기로 미산출이다. 단계 4 는 어휘만 지었고 채굴
공리 인스턴스(`MinedAxiom`)는 0 이므로 "공리 신설" 선행조건에 걸리지 않았다 — 이 해석은 15.1 에 처음 적었고 여기서
재확인한다. `substitutableWith` 일몰은 이 게이트에 묶여 있다.

### 15.8 다음

**단계 7** — V2–V4 재측정. **1단계에서 τ 를 결과를 보기 전에 동결**한다(단계 1 의 V2 기준선 `SPR@100 0.0053` 등은
이미 본 수이므로 τ 의 근거는 그 수가 아니라 §5 V2 의 세 조건과 대조군 tfidf 층별 값에서 끌어낸다). V2 목표 노드를
인용문헌 노드(퇴화형)에서 `disclosure/{publication_id}` 로 교체(§13.5). `substitutableWith` 일몰 판정(PLAN-002 3단계
착수 여부). 별건: citationStatus 파생 · combinableWith · MinedAxiom 실체화 · R7 예외 · FailureMode·Skill 바인딩.
