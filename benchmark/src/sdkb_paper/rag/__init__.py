"""C2′ 전달 실험 — 검색 층의 이득이 생성 층으로 전달되는가 (RQ5 · PLAN-038).

**이 패키지는 RAG 방법을 제안하지 않는다**(PLAN-038 §9 · CLAUDE.md §0.5-4). 생성기는 상수이고
바뀌는 것은 **검색팔 하나**뿐이다 — 같은 벤치마크의 두 번째 판독이지 두 번째 벤치마크가 아니다.

경계 셋.
  1. 새 질의셋·새 qrel·새 분할을 만들지 않는다. 동결 run(`runsets/O_pre_linker/`)만 읽는다.
  2. 검색 코드를 수정하지 않는다 — run 은 **읽기만** 한다.
  3. A층 산출물은 **탐색적**이다(§7 결정 "다"). 판독값을 보고 프롬프트·K 를 고치는 것은 금지이며,
     허용되는 수정은 §12.4 가 열거한 "고장" 넷뿐이다.

모듈.
  `frozen`   — §12.1 동결표와 프롬프트 전문. 상수는 이 파일에만 있다.
  `context`  — 동결 run 상위 K → 컨텍스트 조립(누출 마스크 강제 · 두 팔의 차이는 run 하나뿐).
  `generate` — Bedrock 호출(`make rag`). 원문·usage·서빙 메타데이터 전량 보존.
  `score`    — 인용 식별자 대조(`make rageval`). **결정적** — 같은 입력에 두 번 돌리면 바이트 동일.
"""
